A notebook rendered for the artifact gallery now keeps its remote assets —
inline LaTeX, interactive widgets and Mermaid diagrams all render — unless the
deployment sets `offline: true`, which restores the self-contained render that
needs no network at view time.
