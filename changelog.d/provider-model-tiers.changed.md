The tier-map example in an emitted `profile.yml` shows current model ids.
The `als-apg` provider's `sonnet` and `opus` tiers name `claude-sonnet-5` and
`claude-opus-5`, and `claude-fable-5-1` joins its model list without backing a tier.
