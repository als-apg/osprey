Design system: a shared info tip — a small "?" or "i" disc that shows a
bubble on hover and on keyboard focus — for every interface, used by the
Bluesky queue controls and the control-target popover.
