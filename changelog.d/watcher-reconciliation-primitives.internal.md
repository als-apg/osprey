The interface file watchers gain the shared pieces a reconciliation pass is
built from: a stamp for one path that matches the one a listing of its parent
holds, a timer that runs a pass on an interval and survives a pass that raises,
and the single reader of the interval both watchers use.
