Each shared compose partial now names the golden suites its macros pin, and a
test derives that list from the same sources the templates' own notes come
from, so it cannot go stale. Nothing a deployment sees changes.
