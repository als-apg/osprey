One shared guard now defines what a positive-integer config value is, so a cap,
an interval and a port are read the same way wherever they are read, and the
refusal names the key or the environment variable in one sentence.
