The bundled demo channel database, the demo knowledge graph seeded beside it,
and the generator that grows both describe the quadrupole and sextupole
families as belonging to a storage ring rather than to one named accelerator,
so the CHANNELS panel, the graph search and the model no longer read a
different facility's machine name out of the demo corpus.
