The `control-assistant-readwrite` and `control-assistant-admin` tiers arm
channel writes on the sandbox simulator only; the live machine and its
stand-in stay read-only. The live machine's posture is pinned by name, so no
later per-type edit can arm it, and switching a session to either
hardware-shaped target takes its writes away with no rebuild.
