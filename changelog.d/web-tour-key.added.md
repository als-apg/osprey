`web.tour` — who is offered the onboarding tour, and how often (`once`,
`always`, `never`) — is documented in the configuration reference and carried
as a commented stanza in every shipped preset, alongside the per-user
`OSPREY_WEB_TOUR` a multi-user roster entry's `tour:` field renders.
