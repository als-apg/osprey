A process that only searches — the ARIEL MCP server, or any caller that builds
the search service itself — now finds the registered search modules and can
route to a configured mode, and the capabilities report offers the same modes.
