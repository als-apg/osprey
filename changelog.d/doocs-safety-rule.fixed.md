A DOOCS deployment's control-system safety rule names the DOOCS bypass
routes — `doocs4py.get`, `doocs4py.set` and `doocs4py.subscribe` — instead
of the generic "no direct hardware library calls" text.
