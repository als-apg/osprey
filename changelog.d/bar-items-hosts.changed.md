The web terminal's connection dot and terminal size are now entries in the bar
catalog, so they can be reordered, moved between the header and the status bar,
or left out like any other bar item. Both bars now space their contents by the
same rule: one gap per bar, instead of separate left and right groups.
