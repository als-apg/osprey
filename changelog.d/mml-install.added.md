`osprey mml` installs a facility from its MATLAB Middle Layer export: `import`
reads the export, `map` records what each device family and signal means, and
`emit` writes the middle-layer channel database, the facility ontology, the
knowledge pages and the graph corpus from that one review — so either channel
finder paradigm can be used without a second install.
