The BLUESKY panel no longer offers Start on a queue whose manager reported a
state it does not recognise. The state is named in the button's tooltip, and
on the Plans tab an item added in that condition says it waits its turn rather
than claiming it runs now. Stop stays sendable in every condition and names the
unfamiliar state too.
