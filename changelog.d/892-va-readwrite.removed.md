The `control-assistant-va-readwrite` preset. Its posture is what
`control-assistant-readwrite` now ships, so a roster that points a persona at
the removed name must name `control-assistant-readwrite` instead.
