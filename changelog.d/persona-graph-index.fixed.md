Per-user web terminals of a graph-mode deployment now carry the channel search index. The build kept `services.graphdb.ttl_path` and `index_path` out of every persona render along with the store it does not run, so no index was derived for the per-user images and the CHANNELS tab, the roster and the agent's `search_channels` tool answered "No corpus is configured". Keys that name a file in the render's own data tree now survive into attached renders, and every persona ships an index built from the corpus it stages.
