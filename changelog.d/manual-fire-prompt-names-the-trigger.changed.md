The approval prompt for a `manual_fire` names the trigger the fire would
start, and the payload where the call carries one, so the name to check is on
the prompt rather than in the agent's message above it.
