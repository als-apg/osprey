A profile or persona key written with no entries under it, such as `web_panels:`,
`skills:` or `dependencies:`, now reads as the empty selection instead of failing
`osprey build` with an unexpected-error traceback.
