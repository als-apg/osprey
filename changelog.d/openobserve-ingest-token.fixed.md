`osprey up` provisions the telemetry ingest account against OpenObserve
releases that hand the token over on the create and serve no per-account read,
instead of warning "answered HTTP 405" and leaving the agent's telemetry refused.
