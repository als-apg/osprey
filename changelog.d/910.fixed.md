Attached persona renders keep the graph store's database name
(`services.graphdb.database`) their host sets, beside its address and
account, so sessions against an external store open the database that
holds the corpus instead of the driver's default.
