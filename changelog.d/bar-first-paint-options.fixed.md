The web terminal's bars now paint each item with its configured options, such
as a UTC clock or a fixed-width space, on the first frame. Before, they started
from the defaults and corrected a moment later.
