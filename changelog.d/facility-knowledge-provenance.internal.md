The graph seeder, TTL seeder, OKF document format and demo ontology state their
rules on their own merits instead of quoting one facility's corpus, project and
device identifiers as the justification and the worked examples.
