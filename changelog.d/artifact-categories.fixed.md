An artifact saved under a category the gallery does not know now says what to
do about it: the warning names `artifact_server.categories`, the config key
that registers a facility's own categories, instead of a source file inside the
installed package. The key is documented in the configuration reference and
carried as a commented stanza in every shipped preset.
