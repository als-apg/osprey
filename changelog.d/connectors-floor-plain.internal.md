The osprey-connectors requirement is a plain stable floor. Its `!=` pre-release
exclusion changed nothing pip or uv resolves, and the release skill now says what
pairs a beta framework with its beta connectors: the install's `--pre`.
