`osprey channel-finder benchmark` now hands the provider's gateway URL to the
channel-finder server it spawns, so an `als-apg` project no longer fails every
in-context query with `Base URL required for als-apg`.
