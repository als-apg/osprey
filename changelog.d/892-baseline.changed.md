A `control-assistant` deployment opens its sessions on the sandbox simulator
instead of the live stand-in. The stand-in is still deployed and still owns
the recorded archive, so reaching it is a deliberate target switch.
