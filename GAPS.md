# Extensibility Gaps Backlog

> Auto-generated by `/gaps scan` on 2026-03-03.
> Use `/gaps next` to execute items. Use `/gaps status` to check progress.
>
> Policy: Zero backward compatibility. Flag legacy shims and compat code for removal.

## Summary

- Total items: 35
- Completed: 6
- Remaining: 29
- Critical (P0): 3 | High (P1): 7 | Medium (P2): 15 | Low (P3): 10

## Backlog

### [P0] GP-001: Custom MCP servers bypass ALL safety hooks (completed)

**Severity**: GAP | **Categories**: hard_coded, extension_point, config_flexibility, portability
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_error_guidance.py`, `src/osprey/registry/mcp.py`
**Affects**: All facilities adding custom MCP servers
**Flagged by**: Agents 1, 2, 3, 5

**Problem**: `OSPREY_PREFIXES` is a hard-coded tuple in two hooks — `osprey_approval.py` (3 prefixes) and `osprey_error_guidance.py` (8 prefixes). Tool calls from custom MCP servers added via `claude_code.servers` config bypass:
1. The **approval hook** — custom server writes get no human-in-the-loop approval
2. The **error guidance hook** — custom server errors get no structured guidance
3. There is **no config-driven way** to attach `PreToolUse` hooks to custom servers (`_custom_server_from_spec()` only attaches `PostToolUse` error guidance)

This is a **safety gap**: a facility adding a hardware-write-capable custom server has no config path to get it under the approval workflow.

**Approach**:
1. Generate OSPREY_PREFIXES dynamically from the resolved server list at `osprey claude regen` time
2. Extend `_custom_server_from_spec()` to support `hooks.pre_tool_use` in config spec
3. Expose named hook presets (`approval`, `writes_check`, `limits`) for config references
4. Write OSPREY_PREFIXES and custom hooks into a shared hook config file

**Status**: completed
**Completed**: 2026-03-04 — commit 30aa9f21 (dynamic hook prefixes)

---

### [P0] GP-002: Write detection patterns not configurable — custom protocols bypass approval (completed)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility, portability
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/templates/project/config.yml.j2`, `src/osprey/services/python_executor/analysis/pattern_detection.py`
**Affects**: All non-EPICS facilities
**Flagged by**: Agents 1, 3, 5

**Problem**: Three interrelated issues:
1. `_FALLBACK_WRITE_PATTERNS = ["caput(", "write_channel(", ".put("]` is hard-coded and EPICS-specific — Tango/LabVIEW patterns not caught in fallback path
2. `config.yml.j2` documents `control_system.patterns.write` as user-extendable, but `osprey_approval.py` never reads this config path — the config is aspirational/dead
3. When osprey is not importable (fallback path), only 3 EPICS patterns are checked vs 13+ in the framework module

**Approach**:
1. In `osprey_approval.py` framework path: pass config's `control_system.patterns` to `detect_control_system_operations()`
2. In fallback path: load patterns from `config.yml` directly (config is already loaded)
3. Un-comment `control_system.patterns` in `config.yml.j2` and make it active
4. Add Tango/LabVIEW patterns to `_FALLBACK_WRITE_PATTERNS` for baseline coverage

**Status**: completed
**Completed**: 2026-03-04 — fallback patterns expanded to 15, config-driven overrides, deprecated nested format removed

---

### [P0] GP-003: WRITE_TOOLS kill switch only blocks 2 hardcoded tools (completed)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_writes_check.py`
**Affects**: All facilities with custom write-capable servers
**Flagged by**: Agents 1, 3

**Problem**: `WRITE_TOOLS = {"mcp__controls__channel_write", "mcp__python__execute"}` is hard-coded. When `control_system.writes_enabled: false`, only these 2 tools are blocked. Custom servers with write tools (e.g., `mcp__plc__set_output`) pass through. The global writes kill switch does not actually kill all writes.

**Approach**:
1. Add `control_system.write_tools` config key for facility-specific write tools
2. Generate WRITE_TOOLS at deploy time from server registry (tools with `permissions: ask`)
3. Merge config-specified tools with framework defaults at hook startup

**Status**: completed
**Completed**: 2026-03-04 — dynamic write_tools via hook_config.json, fail-closed fallback to framework defaults

---

### [P1] GP-004: All hook messages hardcoded — not customizable by facilities (resolved)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility, prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_writes_check.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_limits.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_memory_guard.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_error_guidance.py`
**Affects**: All facilities wanting to rebrand or localize
**Flagged by**: Agents 1, 3, 4

**Problem**: Five hooks contain hard-coded user-facing messages with specific wording, emoji, and instructions:
- `OSPREY APPROVAL REQUIRED` — approval prompt
- `WRITES DISABLED` — writes kill switch denial
- `CHANNEL LIMITS VIOLATION` — limits denial
- `WRITE DENIED` — memory guard denial
- `ERROR DETECTED` — error guidance injection

All marked with `PROMPT-PROVIDER` comments acknowledging the debt. Facilities cannot rebrand (e.g., add "Contact shift supervisor at x4321") or localize without editing hook source.

**Approach**:
1. Add `hooks.messages` config section with keys for each message type
2. Hooks load message templates from config with current strings as defaults
3. Support format-string placeholders in templates (e.g., `{channel_list}`)

**Status**: resolved
**Completed**: 2026-03-04 — PROMPT-PROVIDER comments removed, load_osprey_config consolidated into osprey_hook_log.py. Messages are already generic; facilities customize via `osprey eject`.

---

### [P1] GP-005: Safety rules EPICS-only — wrong guidance at non-EPICS facilities (pending)

**Severity**: GAP | **Categories**: prompt_customizability, portability
**Files**: `src/osprey/templates/claude_code/claude/rules/control-system-safety.md`, `src/osprey/templates/claude_code/claude/rules/safety.md`, `src/osprey/templates/claude_code/claude/rules/error-handling.md`
**Affects**: All non-EPICS facilities
**Flagged by**: Agents 4, 5

**Problem**: Three rules files embed EPICS-specific content:
- `control-system-safety.md` is titled "EPICS Channel Access" and only prohibits `epics.caget()`, `epics.caput()`, `PV().put()` — says nothing about Tango, LabVIEW, or OPC-UA
- `safety.md` references "caput, pv.put, write_channel" as patterns to flag
- `error-handling.md` mentions "EPICS channel disconnected" and "archiver appliance status"

A Tango facility gets rules that don't mention Tango as prohibited — an agent could call `tango.DeviceProxy().write_attribute()` without the rule flagging it.

**Approach**:
1. Convert `control-system-safety.md` to `.md.j2` template, parameterize protocol name and prohibited library names from `control_system.type` config
2. Generalize title to "Control System Safety" (drop "EPICS Channel Access")
3. Add prohibited patterns for all supported protocols
4. Use generic example channel names or source from facility context

**Status**: pending

---

### [P1] GP-006: Default provider 'cborg' blocks non-LBNL deployment (done)

**Severity**: GAP | **Categories**: portability
**Files**: `src/osprey/cli/templates/manager.py`, `src/osprey/templates/project/config.yml.j2`, `src/osprey/cli/config_cmd.py`, `src/osprey/cli/project_actions.py`, `src/osprey/interfaces/artifacts/logbook.py`, `src/osprey/services/channel_finder/tools/llm_channel_namer.py`
**Affects**: All non-LBNL facilities
**Flagged by**: Agent 5

**Problem**: The default AI provider is `cborg` (LBNL's internal proxy at `api.cborg.lbl.gov`), hardcoded in 6+ files. Non-LBNL facilities:
1. Get unreachable API URL from `osprey init`
2. Hit silent failures in internal services (logbook composition, LLM channel namer) that also default to `cborg`
3. Must discover and change the provider system manually

**Resolution**: Changed default provider from `cborg` to `anthropic` across 9 production files (11 occurrences) and 4 test files. LBNL users can still set `--provider cborg` or configure it in `config.yml`. The `cborg` provider implementation and all conditional logic remain intact.

**Status**: done

---

### [P1] GP-007: MCP tool docstrings say 'EPICS' — misleads agents at non-EPICS facilities (completed)

**Severity**: GAP | **Categories**: portability, prompt_customizability
**Files**: `src/osprey/mcp_server/control_system/tools/channel_write.py`, `src/osprey/mcp_server/control_system/tools/channel_read.py`, `src/osprey/mcp_server/control_system/tools/archiver_read.py`
**Affects**: All non-EPICS facilities
**Flagged by**: Agent 5

**Problem**: MCP tool docstrings (the LLM-visible descriptions) say "Write values to one or more EPICS control-system channels" and "Read current values from one or more EPICS control-system channels." At a Tango facility, an LLM may conclude these tools are EPICS-specific and attempt to use Tango libraries directly, bypassing safety layers.

**Approach**:
1. Change docstrings to protocol-agnostic: "Write values to one or more control-system channels"
2. Drop "EPICS" from all MCP tool descriptions — protocol is a connector implementation detail

**Status**: completed — commit 3acbb7e5

---

### [P1] GP-008: approval.capabilities config exists but is never read (pending)

**Severity**: GAP | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`, `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/utils/config.py`
**Affects**: All facilities
**Flagged by**: Agent 3

**Problem**: `config.yml.j2` defines `approval.capabilities.python_execution.mode` (disabled|control_writes|all_code) and `approval.capabilities.memory.enabled`, but `osprey_approval.py` only reads `approval.global_mode`. A facility setting `python_execution.mode: all_code` gets no effect. `utils/config.py` builds these defaults but they're never consumed.

**Approach**:
1. Either wire `approval.capabilities` into `osprey_approval.py`'s selective-mode logic
2. Or remove the capabilities sub-section from `config.yml.j2` to avoid misleading users
3. Decide: is per-capability approval mode the right design, or is `global_mode` sufficient?

**Status**: pending

---

### [P1] GP-009: Connector type strings scattered as literals throughout codebase (pending)

**Severity**: GAP | **Categories**: hard_coded, portability
**Files**: `src/osprey/connectors/factory.py`, `src/osprey/mcp_server/control_system/server_context.py`, `src/osprey/services/python_executor/analysis/pattern_detection.py`, `src/osprey/cli/config_cmd.py`, `src/osprey/cli/project_actions.py`
**Affects**: All facilities adding new connector types
**Flagged by**: Agents 1, 5

**Problem**: Connector type names (`"epics"`, `"mock"`, `"tango"`, `"labview"`) appear as string literals in 6+ files. `server_context.py` validates against `("mock", "epics", None)` — a facility using `"tango"` gets a spurious warning. `config_cmd.py` has a hard-coded `Choice` list. Adding a new type requires updating scattered string checks.

**Approach**:
1. Define connector type constants in `osprey.connectors.types` or derive from registry
2. Replace all string literals with constant references
3. Build CLI `Choice` list dynamically from registered connectors
4. Remove `server_context.py` validation (ConnectorFactory already raises clear errors)

**Status**: pending

---

### [P1] GP-010: Channel-finder agent contains ALS-specific terminology (pending)

**Severity**: GAP | **Categories**: prompt_customizability, portability
**Files**: `src/osprey/templates/claude_code/claude/agents/channel-finder.md.j2`, `src/osprey/templates/claude_code/claude/agents/_terminology/*.md.j2`, `src/osprey/templates/claude_code/claude/agents/direct-channel-finder.md.j2`
**Affects**: All non-ALS facilities
**Flagged by**: Agents 4, 5

**Problem**: Channel-finder agent prompts contain ALS-specific knowledge:
- System names: SR=Storage Ring, BR=Booster Ring, BTS=Booster-to-Storage
- Device families: BPM, HCM, VCM, QF, QD, DCCT
- Terminology mappings: "beam current"→DCCT, "corrector magnets"→HCM/VCM
- PV examples: `SR:*:BPM:*`, `SR:01:BPM:01:X`

A fusion experiment or beamline has completely different systems, families, and naming. The `PROMPT-PROVIDER` comments acknowledge this debt.

**Approach**:
1. Make terminology tables config-driven (YAML data structure or facility-provided file)
2. Templates already use Jinja2 — add `facility_terminology` context variable
3. Keep ALS examples only when `facility.type == "als"` or provide generic defaults
4. Allow section-level overrides instead of requiring ejection of the entire 222-line agent file

**Status**: pending

---

### [P2] GP-011: execution_control config section is entirely dead (completed)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`
**Affects**: All facilities (misleading config)
**Flagged by**: Agent 3

**Problem**: The entire `execution_control` section (`limits.max_reclassifications`, `limits.max_planning_attempts`, `limits.max_step_retries`, `limits.max_execution_time_seconds`, `agent_control.orchestration_mode`, `agent_control.task_extraction_bypass_enabled`) is LangGraph-era dead config. Zero Python code reads any of these keys. Config comment confirms: "Not used in Claude Code mode."

**Approach**:
1. Remove the entire `execution_control` section from `config.yml.j2`
2. Remove corresponding defaults from `utils/config.py` if present

**Status**: completed
**Completed**: 2026-03-04 — removed from project template, control_assistant template, test fixtures, and test data

---

### [P2] GP-012: Framework agent catalog closed to config-driven additions (pending)

**Severity**: IMPROVEMENT | **Categories**: extension_point
**Files**: `src/osprey/registry/mcp.py`
**Affects**: Facilities wanting conditional custom agents
**Flagged by**: Agent 2

**Problem**: `resolve_agents()` auto-discovers `.claude/agents/*.md` files but these get only `name` and `description`. They cannot specify `condition` (enable only when a config key is truthy), `server_dependency`, or `template_path`. The `FRAMEWORK_AGENTS` dict is the only path for conditional agents, requiring framework source edits.

**Approach**:
1. Extend `claude_code.agents` config to support full `AgentDefinition` fields
2. When a config entry names an agent not in `FRAMEWORK_AGENTS`, create an `AgentDefinition` from spec

**Status**: pending

---

### [P2] GP-013: PromptCatalog has no registration API for facility artifacts (pending)

**Severity**: IMPROVEMENT | **Categories**: extension_point
**Files**: `src/osprey/services/prompts/catalog.py`
**Affects**: Facilities with custom hooks/skills
**Flagged by**: Agent 2

**Problem**: `PromptCatalog.default()` returns a hardcoded list. No `register()` or `add_artifact()` method. Custom hooks placed in `.claude/hooks/` by a facility are invisible to `osprey prompts list`, `osprey prompts scaffold`, and drift detection.

**Approach**:
1. Add `PromptCatalog.register(artifact)` method
2. Add `from_config(config)` factory merging defaults with facility artifacts
3. Or auto-scan `.claude/hooks/` for unregistered files

**Status**: pending

---

### [P2] GP-014: Timezone defaults to America/Los_Angeles (pending)

**Severity**: IMPROVEMENT | **Categories**: hard_coded, portability
**Files**: `src/osprey/templates/project/config.yml.j2`, `src/osprey/templates/project/env.j2`, `src/osprey/templates/project/env.example.j2`
**Affects**: All non-Pacific-timezone facilities
**Flagged by**: Agents 1, 5

**Problem**: Default timezone is `America/Los_Angeles` (Berkeley Lab). Facilities in Europe (CET), Japan (JST), etc. get wrong timestamps. The `timezone.md.j2` rule uses this for interpreting "today", "this morning" — logbook queries silently use wrong timezone. No setup validation warns about this.

**Approach**:
1. Default to UTC (universally correct) or detect system timezone during `osprey init`
2. Add post-init validation warning if timezone is still the shipped default

**Status**: pending

---

### [P2] GP-015: Coarse prompt override granularity — must eject entire files (pending)

**Severity**: IMPROVEMENT | **Categories**: prompt_customizability, extension_point
**Files**: Multiple prompt/agent/hook files
**Affects**: All facilities wanting minor customizations
**Flagged by**: Agents 2, 4

**Problem**: `osprey prompts scaffold` lets facilities override entire files. Changing one paragraph in a 222-line agent file requires owning the whole thing and manually tracking drift. Only `facility.md` is user-owned; all other files are framework-managed. Agents and skills are explicitly excluded from user-ownership.

**Approach**:
1. Implement section-level overrides for `PROMPT-PROVIDER` sections
2. Allow facilities to provide override snippets that get merged into templates at render time
3. Consider user-ownership for rules files (not agents, which mix framework logic with facility content)

**Status**: pending

---

### [P2] GP-016: python_executor timeout not in config template (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`, `src/osprey/mcp_server/python_executor/executor.py`
**Affects**: Facilities running long computations
**Flagged by**: Agent 3

**Problem**: `python_executor.execution_timeout_seconds` defaults to 600s in code but has no entry in `config.yml.j2`. Facilities needing longer timeouts for beam dynamics simulations must know the internal key name.

**Approach**:
1. Add `python_executor` section to `config.yml.j2` with `execution_timeout_seconds: 600`

**Status**: pending

---

### [P2] GP-017: Hook timeouts hardcoded in registry (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/registry/mcp.py`
**Affects**: Facilities with slow network paths
**Flagged by**: Agent 3

**Problem**: Hook timeouts are Python constants: approval=5s, writes_check=5s, limits=10s, error_guidance=5s, cf_feedback=10s. Facilities with slow network paths (e.g., VPN to control systems) may need longer timeouts. No config path to override.

**Approach**:
1. Add optional `claude_code.hook_timeouts` config section
2. Read in `resolve_servers()` to override `HookEntry.timeout` values

**Status**: pending

---

### [P2] GP-018: Framework server env vars cannot be extended via config (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/registry/mcp.py`
**Affects**: Facilities needing facility-specific env vars on framework servers
**Flagged by**: Agent 3

**Problem**: When overriding a framework server in `claude_code.servers`, only `enabled: true/false` is supported. Cannot add env vars without disabling the server and re-defining it as custom (losing hooks, permissions, etc.).

**Approach**:
1. In `resolve_servers()`, when override dict contains `env`, merge into server's env dict
2. This is additive, not destructive

**Status**: pending

---

### [P2] GP-019: Sandbox executor has EPICS-specific dangerous patterns (pending)

**Severity**: IMPROVEMENT | **Categories**: hard_coded
**Files**: `src/osprey/mcp_server/workspace/execution/sandbox_executor.py`
**Affects**: Non-EPICS facilities (false positives) and facilities with additional control system libraries
**Flagged by**: Agent 1

**Problem**: `_DANGEROUS_PATTERNS` includes `("epics", "epics module")`, `("caput", "caput()")`, `("PV.put", "PV.put()")`. Non-EPICS facilities get false positives; facilities with additional control libraries can't add patterns.

**Approach**:
1. Add `sandbox.dangerous_patterns` config key
2. Merge with framework defaults at module init
3. Or derive control-system-specific patterns from configured connector type

**Status**: pending

---

### [P2] GP-020: Mock backend generates ALS-specific PV naming (pending)

**Severity**: IMPROVEMENT | **Categories**: portability
**Files**: `src/osprey/services/channel_finder/backends/mock.py`
**Affects**: All non-ALS facilities using mock mode
**Flagged by**: Agent 5

**Problem**: Mock backend uses `_HOST = 'als-srv01.lbl.gov'`, ALS-style PV naming (`SR:{sector}:{device}:{id}:{signal}`), and ALS device families (BPM, HCM, VCM, QF, QD, DCCT, RF). Non-ALS facilities find mock data confusing.

**Approach**:
1. Make mock backend device types, naming pattern, and hostname configurable
2. Provide facility-neutral defaults (e.g., `SYS:SEC01:DEV01:Signal`)
3. Move ALS-specific mock data to an ALS preset

**Status**: pending

---

### [P2] GP-021: connector.mock.simulate_delays config key mismatch (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`, `src/osprey/connectors/control_system/mock_connector.py`
**Affects**: All facilities using mock connector
**Flagged by**: Agent 3

**Problem**: `config.yml.j2` documents `connector.mock.simulate_delays` (bool), but `mock_connector.py` reads `response_delay_ms` (int, default 10) and `noise_level` (float, default 0.01). `simulate_delays` is never read.

**Approach**:
1. Replace `simulate_delays: false` with `response_delay_ms: 10` and `noise_level: 0.01` in config template
2. Or add `simulate_delays` support in `mock_connector.py` (if false, set delay to 0)

**Status**: pending

---

### [P2] GP-022: Lattice physics rules contain ALS-specific working point values (pending)

**Severity**: IMPROVEMENT | **Categories**: prompt_customizability, portability
**Files**: `src/osprey/templates/claude_code/claude/rules/lattice-physics.md.j2`
**Affects**: All non-ALS using lattice_design template
**Flagged by**: Agents 4, 5

**Problem**: Includes "ALS Working Point" section with hardcoded tune values (nu_x ~ 14.25, nu_y ~ 8.18). Any other accelerator has completely different values.

**Approach**:
1. Move machine-specific parameters to config or facility data file
2. Template can read from `machine_params` context variable
3. Keep ALS values only in ALS machine data files

**Status**: pending

---

### [P2] GP-023: Only EPICS and Mock connector implementations exist (pending)

**Severity**: IMPROVEMENT | **Categories**: portability
**Files**: `src/osprey/connectors/control_system/epics_connector.py`, `src/osprey/connectors/control_system/mock_connector.py`
**Affects**: All non-EPICS facilities
**Flagged by**: Agent 5

**Problem**: Despite architecture supporting protocol-agnostic connectors, only EPICS has a real implementation. Factory docs mention "tango" but no `TangoConnector` exists. OSPREY is effectively EPICS-only for production.

**Approach**:
1. Provide skeleton Tango/LabVIEW connectors with TODO markers, or
2. Remove "tango"/"labview" from docs until implementations exist, or
3. Create `contrib/` directory for community connectors
4. Update documentation to be honest about current protocol support

**Status**: pending

---

### [P2] GP-024: Provider registry requires source changes to add providers (pending)

**Severity**: IMPROVEMENT | **Categories**: portability
**Files**: `src/osprey/models/provider_registry.py`
**Affects**: Facilities with custom API proxies
**Flagged by**: Agent 5

**Problem**: `_BUILTIN_PROVIDERS` and `PROVIDER_API_KEYS` are closed dicts. `register_provider()` exists for runtime registration but there's no config-driven path. A facility with a custom API proxy must fork source, write startup code, or misconfigure as an existing provider type.

**Approach**:
1. Support config-driven provider registration under `api.providers`
2. Wire existing `register_provider()` API to config loading

**Status**: pending

---

### [P2] GP-025: epics_writes_enabled backward-compat shim (pending)

**Severity**: IMPROVEMENT | **Categories**: hard_coded (legacy_remove)
**Files**: `src/osprey/services/python_executor/execution/control.py`, `src/osprey/utils/config.py`
**Affects**: Codebase cleanliness
**Flagged by**: Agent 1

**Problem**: `ExecutionControlConfig` maintains deprecated `epics_writes_enabled` field alongside `control_system_writes_enabled`, with `__post_init__` backward-compat shim. `utils/config.py` still exports the deprecated key. Per zero backward-compat policy, should be removed.

**Approach**:
1. Remove `epics_writes_enabled` field from `ExecutionControlConfig`
2. Remove from `_build_agent_control_defaults()` in `config.py`
3. Update any remaining consumers

**Status**: pending

---

### [P3] GP-026: settings.json permissions not facility-configurable (pending)

**Severity**: UX | **Categories**: prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/settings.json.j2`
**Affects**: Facilities wanting different permission policies
**Flagged by**: Agent 4

**Problem**: The deny list (Bash, Edit, WebFetch, etc.) and allow/ask lists are generated from server registry but the deny list is hardcoded. Facilities may need to allow currently-denied tools or deny additional tools.

**Approach**:
1. Add `claude_code.permissions` config for additional allow/deny/ask entries
2. Merge with framework defaults during template rendering

**Status**: pending

---

### [P3] GP-027: ALS branding in lattice evaluation scripts (pending)

**Severity**: UX | **Categories**: prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/skills/lattice-evaluation/references/frequency_map.py`, `src/osprey/templates/claude_code/claude/skills/lattice-evaluation/references/resonance_diagram.py`, `src/osprey/templates/claude_code/claude/skills/lattice-evaluation/references/dynamic_aperture.py`, `src/osprey/templates/claude_code/CLAUDE.md.j2`
**Affects**: Non-ALS facilities using lattice_design template
**Flagged by**: Agent 4

**Problem**: Reference scripts use "ALS" in artifact titles ("ALS Frequency Map Analysis", etc.). `CLAUDE.md.j2` hardcodes "The demo lattice is the ALS storage ring."

**Approach**:
1. Replace "ALS" with facility_name variable in templates
2. Source lattice file path and description from config

**Status**: pending

---

### [P3] GP-028: Eject command only supports services (pending)

**Severity**: UX | **Categories**: extension_point
**Files**: `src/osprey/cli/eject_cmd.py`
**Affects**: Facilities wanting to customize hooks, connectors, or agents
**Flagged by**: Agent 2

**Problem**: `osprey eject` only has `list` and `service` subcommands. No mechanism to eject hooks, connectors, or agent templates. Facilities must manually find and copy template files.

**Approach**:
1. Add `osprey eject hook <name>`, `osprey eject connector <name>`, `osprey eject agent <name>`
2. Use PromptCatalog to resolve template paths

**Status**: pending

---

### [P3] GP-029: artifact_server config missing from template (pending)

**Severity**: UX | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`
**Affects**: Facilities deploying artifact gallery on non-default port
**Flagged by**: Agent 3

**Problem**: `osprey_approval.py` and other modules read `artifact_server` config (host, port), but `config.yml.j2` doesn't include it. No visible config path in the standard template.

**Approach**:
1. Add `artifact_server` section to `config.yml.j2` with `host: 127.0.0.1`, `port: 8086`

**Status**: pending

---

### [P3] GP-030: ConnectorFactory defaults to 'epics' instead of 'mock' (pending)

**Severity**: UX | **Categories**: hard_coded
**Files**: `src/osprey/connectors/factory.py`
**Affects**: New facilities with incomplete config
**Flagged by**: Agent 1

**Problem**: `ConnectorFactory.create_control_system_connector` defaults to `"epics"` when no `control_system.type` is set. Surprising for facilities without EPICS infrastructure.

**Approach**:
1. Change default to `"mock"` (safe, no hardware) or raise error when not configured
2. The project template already sets `type: mock` explicitly

**Status**: pending

---

### [P3] GP-031: ALS-specific URLs and references scattered in source (pending)

**Severity**: UX | **Categories**: hard_coded, portability
**Files**: `src/osprey/services/ariel_search/ingestion/adapters/als.py`, `src/osprey/utils/config_writer.py`, `src/osprey/templates/apps/control_assistant/config.yml.j2`
**Affects**: Non-LBNL facilities
**Flagged by**: Agents 1, 5

**Problem**: Scattered LBNL-specific references:
- ALS logbook URL hardcoded (`elog.als.lbl.gov`)
- ALS gateway address comparison in `config_writer.py`
- Confluence URL defaults to `commons.lbl.gov`

**Approach**:
1. Make URLs configurable via adapter config
2. Remove hardcoded gateway comparison in `config_writer.py`
3. Use placeholder URLs in templates

**Status**: pending

---

### [P3] GP-032: Minor ALS examples in prompt templates (pending)

**Severity**: UX | **Categories**: prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/output-styles/control-operator.md.j2`, `src/osprey/templates/claude_code/claude/rules/data-visualization.md`, `src/osprey/templates/claude_code/claude/agents/matlab-search.md.j2`, `src/osprey/templates/claude_code/claude/skills/session-report/reference.md`
**Affects**: Non-ALS facilities (cosmetic)
**Flagged by**: Agent 4

**Problem**: Various minor ALS-specific references: `SR:C01-MG:PS1` in output style example, "EPICS channel access" in data-viz rule, ALS MML group names in MATLAB search, `SR:C03-MG:PS` in session report reference.

**Approach**:
1. Use generic channel name placeholders
2. Replace "EPICS channel access" with "control system channel access"

**Status**: pending

---

### [P3] GP-033: cli.custom_theme and cli.banner commented-out but functional (pending)

**Severity**: UX | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`
**Affects**: All facilities (hidden feature)
**Flagged by**: Agent 3

**Problem**: `cli.custom_theme` and `cli.banner` are commented out in config template but actually wired in code. Users won't discover these features.

**Approach**:
1. Un-comment these sections with example values
2. Mark as optional with descriptive comments

**Status**: pending

---

### [P3] GP-034: LBNL-specific providers cluttering config template (pending)

**Severity**: UX | **Categories**: portability
**Files**: `src/osprey/templates/project/config.yml.j2`
**Affects**: Non-LBNL facilities (confusing)
**Flagged by**: Agent 5

**Problem**: Generated config includes `cborg`, `amsc`, `als-apg`, `stanford`, `argo` as first-class provider entries alongside universal providers. Clutters config and suggests OSPREY is LBNL-internal.

**Approach**:
1. Only include universally accessible providers as uncommented defaults
2. Move facility-specific providers to a commented-out section or generate conditionally

**Status**: pending

---

### [P3] GP-035: Minor portability friction items (pending)

**Severity**: UX | **Categories**: hard_coded, config_flexibility, portability
**Files**: Various
**Affects**: Various
**Flagged by**: Agents 1, 3, 5

**Problem**: Aggregated minor items:
- `CAPTURE_TOOLS` in `osprey_cf_feedback_capture.py` hardcoded to 2 tools
- `textbook-expert` agent missing from `AGENT_DEFAULT_TIERS`
- `EPICS_CA_ADDR_LIST` env var unconditionally set in controls server
- Prompts catalog describes safety rule as "EPICS channel access safety rules"
- `server_context.py` validates against incomplete connector type set
- `pattern_detection.py` defaults connector type to "epics" for logging
- Channel finder feedback store docstring shows "ALS" as example
- Release workflow references `als-apg` GitHub organization

**Approach**:
1. Fix each individually as encountered, or batch in a portability sweep
2. Most are one-line changes (string literal updates)

**Status**: pending
