# Extensibility Gaps Backlog

> Auto-generated by `/gaps scan` on 2026-03-05.
> Use `/gaps next` to execute items. Use `/gaps status` to check progress.
>
> Policy: Zero backward compatibility. Flag legacy shims and compat code for removal.

## Summary

- Total items: 28
- Completed: 1
- Remaining: 27
- Critical (P0): 3 | High (P1): 12 | Medium (P2): 8 | Low (P3): 5

---

## Backlog

### [P0] GP-001: Write detection patterns not extensible for non-EPICS protocols (done)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility, portability
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/mcp_server/workspace/execution/sandbox_executor.py`
**Affects**: All non-EPICS facilities (Tango, OPC-UA, LabVIEW, DOOCS)

**Problem**: Three independent gaps compound into a safety-critical hole:
1. The fallback `_FALLBACK_WRITE_PATTERNS` in the approval hook only covers EPICS/Tango/LabVIEW patterns. When the framework import fails, patterns are hard-coded.
2. The primary framework path (`detect_control_system_operations()`) does NOT merge config-driven patterns — the `control_system.patterns.write` config key is documented in config.yml.j2 but only read by the fallback branch, never by the framework branch.
3. The sandbox executor's `_DANGEROUS_PATTERNS` blocklist only blocks EPICS identifiers (`epics`, `caput`, `PV.put`). Since sandbox tools are auto-approved, a Tango facility's `DeviceProxy.write_attribute()` executes without any safety gate.

**Approach**:
1. Make `detect_control_system_operations()` accept and merge config-driven patterns
2. Wire `control_system.patterns.write` from config.yml into both the approval hook framework path AND the sandbox executor
3. Add protocol-specific dangerous patterns for Tango/OPC-UA/LabVIEW to the sandbox executor, loaded from config
4. Remove the fallback branch — under zero-compat policy, hooks always run in the venv

**Status**: done (73cd286d)

---

### [P0] GP-002: ConnectorFactory silently defaults to EPICS when type is missing (pending)

**Severity**: GAP | **Categories**: hard_coded, portability
**Files**: `src/osprey/connectors/factory.py`
**Affects**: All non-EPICS facilities

**Problem**: `ConnectorFactory` defaults to `types.EPICS` when `control_system.type` is missing from config (line 109). The config template (`config.yml.j2`) defaults to `mock`, creating a disagreement: the template says mock but the code defaults to EPICS. A non-EPICS facility that omits the type key gets confusing pyepics import errors instead of safe mock behavior.

**Approach**:
1. Change factory default from `types.EPICS` to `types.MOCK` to match the template
2. Add a clear warning when defaulting: "No control_system.type configured, using mock connector"
3. Similarly fix archiver default from `types.EPICS_ARCHIVER` to a safe fallback

**Status**: pending

---

### [P0] GP-003: Approval/denial messages hard-coded across all hooks (pending)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility, prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_writes_check.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_limits.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_memory_guard.py`, `src/osprey/templates/claude_code/claude/hooks/osprey_error_guidance.py`
**Affects**: All facilities wanting to rebrand, localize, or customize messaging

**Problem**: All 5 hook scripts contain hard-coded English message strings with emoji:
- "OSPREY APPROVAL REQUIRED" (approval hook)
- "WRITES DISABLED" (writes-check hook)
- "CHANNEL LIMITS VIOLATION" (limits hook)
- "WRITE DENIED" (memory guard hook)
- Error guidance injection text (error guidance hook)

A facility cannot rebrand (replace "OSPREY"), localize to another language, add facility-specific escalation contacts, or customize the messaging tone without editing framework template source.

**Approach**:
1. Add `hooks.messages` config section with keys for each message type
2. Each hook loads its message from config, falling back to current defaults
3. Support template variables in messages (e.g., `{facility_name}`, `{channel_name}`)
4. Include `escalation_contact` config key for error guidance

**Status**: pending

---

### [P1] GP-004: Limits hook hardcodes single tool name (pending)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_limits.py`
**Affects**: Facilities with renamed or additional control servers

**Problem**: The limits hook checks `tool_name != 'mcp__controls__channel_write'` as a hardcoded string (line 73). Unlike the writes-check hook which loads tool names from `hook_config.json`, the limits hook uses a single hardcoded string. If a facility renames the controls server or adds a second control system server, limits validation silently skips the new server's write tool.

**Approach**:
1. Add `limits_tools` key to `hook_config.json.j2` (similar to `write_tools`)
2. Generate from server registry at deploy time
3. Limits hook reads the list instead of hardcoding one tool name

**Status**: pending

---

### [P1] GP-005: Framework server overrides cannot add env vars or hooks (pending)

**Severity**: GAP | **Categories**: extension_point, config_flexibility
**Files**: `src/osprey/registry/mcp.py`
**Affects**: All facilities needing to customize framework servers

**Problem**: When a facility references an existing framework server in `claude_code.servers` (e.g., `controls`), the override path (lines 393-398) only supports toggling `enabled/disabled`. A facility cannot:
- Add extra env vars to the controls server (e.g., `MY_CUSTOM_DB_URL`)
- Append custom pre-approval hooks
- Modify permissions
Custom servers support all these features, but framework server overrides do not.

**Approach**:
1. In `resolve_servers()`, when overriding a framework server, deep-merge spec fields into the cloned `ServerDefinition`
2. Support `env` merge, `hooks.pre_tool_use` append, and `permissions` merge
3. Pattern: `claude_code.servers.controls.env.MY_VAR: value` adds to controls server env

**Status**: pending

---

### [P1] GP-006: Selective approval mode tools are hardcoded (pending)

**Severity**: GAP | **Categories**: hard_coded, config_flexibility
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`, `src/osprey/templates/project/config.yml.j2`
**Affects**: Facilities with custom MCP tools requiring approval

**Problem**: In `selective` approval mode, only `channel_write` and `execute` are checked (hardcoded in the approval hook's `if` chain, lines 237 and 251). Facilities cannot add custom MCP tools to the selective approval list (e.g., a `database_write` tool) without switching to `all_capabilities` mode, which is too broad.

**Approach**:
1. Add `approval.selective_tools: [channel_write, execute]` config key
2. Approval hook reads this list and checks against it
3. Default preserves current behavior

**Status**: pending

---

### [P1] GP-007: PromptCatalog has no extension mechanism for custom artifacts (pending)

**Severity**: GAP | **Categories**: extension_point, prompt_customizability
**Files**: `src/osprey/services/prompts/catalog.py`, `src/osprey/cli/templates/claude_code.py`
**Affects**: Facilities with custom hooks, agents, or skills

**Problem**: `PromptCatalog.default()` returns `_get_default_artifacts()` which is a hard-coded list. Custom hooks deployed by a facility are completely invisible to `osprey prompts list`, the ownership system, and `osprey prompts diff`. Additionally, `is_user_owned()` explicitly prevents agent files from being user-owned (line 210-211), contradicting the claim workflow.

**Approach**:
1. Add `PromptCatalog.register(artifact)` and `PromptCatalog.from_config()` for facility artifacts
2. Auto-discover artifacts from `.claude/hooks/`, `.claude/agents/`, `.claude/skills/`
3. Remove the hard-coded agent exclusion in `is_user_owned()` or document clearly

**Status**: pending

---

### [P1] GP-008: PROMPT-PROVIDER sections not wired to provider system (pending)

**Severity**: GAP | **Categories**: hard_coded, prompt_customizability
**Files**: `src/osprey/templates/claude_code/claude/agents/channel-finder.md.j2`, `src/osprey/templates/claude_code/claude/rules/error-handling.md`, `src/osprey/templates/claude_code/claude/rules/safety.md`
**Affects**: All facilities wanting section-level prompt customization

**Problem**: 7+ `PROMPT-PROVIDER:` annotations across agent and rule templates mark sections that should be sourced from a `FrameworkPromptProvider` but are currently inlined as static text. Sections include: `channel_finding_middle_layer`, `channel_finding_hierarchical`, `channel_finding_in_context`, `facility_context`, `channel_finding_guidelines`, `error_handling_rules`, `safety_rules_file`. None are overridable without owning the entire file.

**Approach**:
1. Implement `FrameworkPromptProvider` as described in the annotations
2. Use Jinja2 include blocks or config-driven content injection for section-level overrides
3. Allow per-section override files (e.g., `.claude/overrides/channel-finder/common-patterns.md`)

**Status**: pending

---

### [P1] GP-009: ALS-specific channel naming examples in agent prompts (pending)

**Severity**: GAP | **Categories**: prompt_customizability, portability
**Files**: `src/osprey/templates/claude_code/claude/agents/channel-finder.md.j2`, `src/osprey/templates/claude_code/claude/agents/direct-channel-finder.md.j2`, `src/osprey/templates/claude_code/claude/agents/_terminology/middle_layer.md.j2`
**Affects**: All non-synchrotron facilities, all facilities with different naming conventions

**Problem**: Agent prompts contain ALS-specific accelerator naming: `SR=Storage Ring`, `BPM=Beam Position Monitors`, `HCM=Horizontal Corrector Magnets`, `QF/QD` families. A fusion experiment, linac, or FEL with different device families gets misleading AI guidance. The "Common Patterns" section (lines 58-67) is pure ALS domain knowledge in a framework file.

**Approach**:
1. Source example patterns from the facility's channel database at `osprey claude regen` time
2. Use the `naming_summary` module to inject real examples from the configured database
3. Fall back to generic patterns (e.g., `AREA:DEVICE:01:SIGNAL`) when no database is configured

**Status**: pending

---

### [P1] GP-010: LBNL-specific providers hardcoded in CLI and config template (pending)

**Severity**: GAP | **Categories**: portability
**Files**: `src/osprey/models/provider_registry.py`, `src/osprey/cli/init_cmd.py`, `src/osprey/templates/project/config.yml.j2`
**Affects**: All non-LBNL facilities

**Problem**: `cborg`, `als-apg`, `amsc`, `argo`, and `stanford` are LBNL/DOE-specific API proxy services hardcoded as built-in providers. The `osprey init --provider` flag lists them as options. `config.yml.j2` includes them as uncommented provider definitions with hardcoded LBNL URLs (e.g., `api.cborg.lbl.gov`). A DESY or SLAC user sees unfamiliar providers and LBNL-specific URLs in their generated config.

**Approach**:
1. Keep only universal providers (anthropic, openai, google, ollama) as built-in
2. Move facility-specific providers to a `contrib` or `presets` system
3. Generate `api.providers` section conditionally based on selected provider
4. Show facility-specific providers only if their API key env var is set

**Status**: pending

---

### [P1] GP-011: epics_writes_enabled backward-compat naming in execution control (pending)

**Severity**: GAP | **Categories**: portability, hard_coded
**Files**: `src/osprey/services/python_executor/execution/control.py`, `src/osprey/utils/config.py`
**Affects**: All non-EPICS facilities

**Problem**: `ExecutionControlConfig` uses `epics_writes_enabled` (line 171) as a deprecated field that still drives logic (line 229). The `validate()` method emits `WARNING: epics.writes_enabled=true (live EPICS writes enabled!)` even on a Tango facility. `config.py` still exports the EPICS-named key. This is backward-compat naming that should be removed under zero-compat policy.

**Approach**:
1. Remove `epics_writes_enabled` field entirely
2. Replace with `control_system_writes_enabled` as the sole field
3. Update `validate()` to use protocol-agnostic messaging
4. Update `config.py` to stop exporting the EPICS-named key

**Status**: pending

---

### [P1] GP-012: Writes-check hook has hardcoded tool names and backward-compat fallback (pending)

**Severity**: GAP | **Categories**: hard_coded
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_writes_check.py`
**Affects**: Facilities with renamed or additional servers

**Problem**: Two issues:
1. `mcp__python__execute` is hardcoded for readonly bypass (line 90-91) instead of coming from `hook_config.json`
2. `_FALLBACK_WRITE_TOOLS` (lines 59-62) is a backward-compat shim for when `hook_config.json` is missing

**Approach**:
1. Add `execute_tools` or `readonly_bypass_tools` list to `hook_config.json`
2. Remove `_FALLBACK_WRITE_TOOLS` fallback — fail-closed when `hook_config.json` is missing (it's always generated during deploy)

**Status**: pending

---

### [P1] GP-013: Custom server hooks only support pre_tool_use presets (pending)

**Severity**: GAP | **Categories**: extension_point
**Files**: `src/osprey/registry/mcp.py`
**Affects**: Facilities needing custom hook pipelines

**Problem**: `_custom_server_from_spec()` resolves pre-tool-use hooks from a fixed `HOOK_PRESETS` dict containing only `approval`, `writes_check`, and `limits`. Two gaps: (1) `post_tool_use` hooks are hard-coded to `_post_error` only and cannot be configured. (2) `pre_tool_use` only accepts preset names — a facility cannot specify a custom hook command via config.

**Approach**:
1. Extend hooks config to support arbitrary commands: accept inline objects `{command: '...', timeout: N}`
2. Add `post_tool_use` config support
3. Expand `HOOK_PRESETS` to include `error_guidance` and `cf_feedback`

**Status**: pending

---

### [P1] GP-014: Pattern detection dual-path config inconsistency (pending)

**Severity**: GAP | **Categories**: config_flexibility
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_approval.py`
**Affects**: All facilities using custom write patterns

**Problem**: `config.yml.j2` documents `control_system.patterns.write` as configurable. The fallback `has_write_patterns()` (used when osprey is not importable) reads this config. But the primary path (when osprey IS importable, line 111) calls `detect_control_system_operations()` without passing config patterns. The framework path reads its own config independently. If hook config and framework config disagree, behavior differs silently.

**Approach**:
1. Pass config-loaded patterns to `detect_control_system_operations()` via the `patterns` parameter in the framework branch
2. Or remove the commented-out config section if the framework auto-discovers patterns

**Status**: pending

---

### [P1] GP-015: EPICS_CA_ADDR_LIST env var injected unconditionally (pending)

**Severity**: GAP | **Categories**: portability
**Files**: `src/osprey/registry/mcp.py`
**Affects**: All non-EPICS facilities

**Problem**: The controls MCP server definition unconditionally injects `EPICS_CA_ADDR_LIST` as an environment variable (line 111), regardless of whether the facility uses EPICS. A Tango or OPC-UA facility gets a meaningless EPICS env var in their `settings.json`.

**Approach**:
1. Make the env dict conditional on `control_system.type`
2. Only inject `EPICS_CA_ADDR_LIST` when the configured connector is `epics`
3. Can be done in `_server_to_dict` or via a condition callback

**Status**: pending

---

### [P2] GP-016: Hook timeouts are hardcoded constants (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/registry/mcp.py`
**Affects**: Facilities with slow validation backends

**Problem**: Hook timeouts are fixed at 5s (approval, writes_check, error_guidance) and 10s (limits, cf_feedback) as Python constants. No config key exists to override per-hook timeouts for facilities that need longer timeouts (e.g., limits validation against a remote database).

**Approach**:
1. Add `claude_code.hook_timeouts` dict mapping hook preset name to timeout seconds
2. Read from config in `HOOK_PRESETS` construction, override defaults

**Status**: pending

---

### [P2] GP-017: Container execution config keys missing from config.yml.j2 (pending)

**Severity**: IMPROVEMENT | **Categories**: config_flexibility
**Files**: `src/osprey/mcp_server/python_executor/executor.py`, `src/osprey/templates/project/config.yml.j2`
**Affects**: Facilities using containerized code execution

**Problem**: The executor reads `python_executor.execution_timeout_seconds`, `services.jupyter.containers.read/write`, and `execution.modes.*.kernel_name` from config, but none appear in `config.yml.j2`. Facilities have no documented way to configure container endpoints or execution timeouts.

**Approach**:
1. Add commented-out `python_executor:` section to config.yml.j2
2. Document `services.jupyter.containers` with default values

**Status**: pending

---

### [P2] GP-018: Eject command only supports services (pending)

**Severity**: IMPROVEMENT | **Categories**: extension_point
**Files**: `src/osprey/cli/eject_cmd.py`
**Affects**: Facilities wanting to customize connectors or MCP servers

**Problem**: The eject command has only `list` and `service` subcommands. No `eject connector`, `eject mcp-server`, or `eject ariel-module` exists. The migration docs reference `osprey eject capability` which doesn't exist. `eject list` only shows services, missing connectors, ARIEL modules, and other component types from the registry.

**Approach**:
1. Add `eject connector <name>` and `eject mcp-server <name>` subcommands
2. Extend `eject list` to show all component types
3. Fix stale `eject capability` references in migration docs

**Status**: pending

---

### [P2] GP-019: Coarse prompt override granularity (pending)

**Severity**: IMPROVEMENT | **Categories**: prompt_customizability
**Files**: `src/osprey/cli/prompts_cmd.py`, `src/osprey/cli/templates/claude_code.py`
**Affects**: All facilities needing partial customization

**Problem**: `osprey prompts claim` operates at whole-file granularity. If a facility needs to change 10 lines in a 222-line channel-finder agent, they must own the entire file and manually track drift on every upgrade. The drift detection alerts on changes but provides no merge assistance.

**Approach**:
1. Implement section-level override points using Jinja2 blocks or include mechanisms
2. Allow per-section override files (e.g., `.claude/overrides/channel-finder/common-patterns.md`)
3. Framework templates splice in overrides during rendering

**Status**: pending

---

### [P2] GP-020: Channel finder feedback capture hardcodes tool names (pending)

**Severity**: IMPROVEMENT | **Categories**: hard_coded
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_cf_feedback_capture.py`
**Affects**: Facilities with renamed or extended channel finder servers

**Problem**: `CAPTURE_TOOLS = {"mcp__channel-finder__build_channels", "mcp__channel-finder__get_channels"}` is hardcoded. If the channel finder server is renamed or extended with new tools, feedback capture silently stops working.

**Approach**:
1. Add `feedback_capture_tools` list to `hook_config.json`
2. Generate from channel-finder server definition in the registry

**Status**: pending

---

### [P2] GP-021: Error class taxonomy not extensible (pending)

**Severity**: IMPROVEMENT | **Categories**: hard_coded
**Files**: `src/osprey/templates/claude_code/claude/hooks/osprey_error_guidance.py`
**Affects**: Facilities with custom MCP servers returning custom error types

**Problem**: `ERROR_CLASS_MAP` maps OSPREY `error_type` strings to human-readable classes via a fixed dict. Custom error types (e.g., `interlock_error`, `beam_loss_error`) get classified as `Internal` (fallback). No config extension mechanism exists.

**Approach**:
1. Allow extension via config key (e.g., `hooks.error_class_map`)
2. Or include in `hook_config.json` for facility customization

**Status**: pending

---

### [P2] GP-022: ALS logbook adapter hardcodes attachment URL (pending)

**Severity**: IMPROVEMENT | **Categories**: portability
**Files**: `src/osprey/services/ariel_search/ingestion/adapters/als.py`
**Affects**: All non-LBNL facilities using the ALS adapter pattern

**Problem**: `ALSLogbookAdapter` hardcodes `https://elog.als.lbl.gov/` as the attachment URL prefix (line 96) rather than reading from config. Even a different LBNL group would get wrong attachment URLs.

**Approach**:
1. Add `attachment_url_prefix` field to ingestion config
2. Fall back to source_url domain if not provided
3. Remove hardcoded ALS URL

**Status**: pending

---

### [P2] GP-023: Mock backend generates ALS-style synchrotron PVs only (pending)

**Severity**: IMPROVEMENT | **Categories**: portability
**Files**: `src/osprey/services/channel_finder/backends/mock.py`
**Affects**: All non-synchrotron facilities testing with mock mode

**Problem**: Mock backend hardcodes synchrotron device types (BPM, HCM, VCM, QF, QD, DCCT, RF) with `SR:` prefix naming. A linac, FEL, or fusion facility testing with mock mode sees only synchrotron-style PVs, misrepresenting their naming patterns.

**Approach**:
1. Make mock backend accept device type table and naming pattern from config
2. Provide presets for different facility types (synchrotron, linac, FEL)
3. Or use curated channel database as mock source when configured

**Status**: pending

---

### [P3] GP-024: ALS ChannelFinder backend naming embeds facility name (pending)

**Severity**: UX | **Categories**: portability
**Files**: `src/osprey/services/channel_finder/backends/als_channel_finder.py`, `src/osprey/mcp_server/direct_channel_finder/server_context.py`
**Affects**: Non-ALS facilities using EPICS ChannelFinder

**Problem**: Backend type is named `als_channel_finder` with ALS-specific class names and a default URL pointing to `localhost:8443/ChannelFinder`. A non-ALS facility using the community EPICS ChannelFinder must use the `als_channel_finder` type name.

**Approach**:
1. Rename to `channelfinder_rest` to reflect the generic EPICS ChannelFinder protocol
2. Remove the `als_` prefix from the module name
3. Keep ALS as one configuration example

**Status**: pending

---

### [P3] GP-025: Facility presets only include APS and ALS (pending)

**Severity**: UX | **Categories**: portability
**Files**: `src/osprey/templates/data/facility_presets.py`, `src/osprey/utils/config_writer.py`
**Affects**: All EPICS facilities except APS/ALS

**Problem**: `FACILITY_PRESETS` contains only `aps`, `als`, and `simulation`. `config_writer.py` uses the ALS gateway address as a sentinel value for facility detection (line 531). Other EPICS facilities have no preset.

**Approach**:
1. Make facility presets extensible via config or plugin mechanism
2. Remove hardcoded ALS address comparison in `config_writer.py`
3. Use preset registry for detection instead of string comparison

**Status**: pending

---

### [P3] GP-026: Dead config keys (simulate_delays, file_paths) (pending)

**Severity**: UX | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`
**Affects**: All facilities (config clutter)

**Problem**: `control_system.connector.mock.simulate_delays: false` is defined in config.yml.j2 but never read by any code. `file_paths.executed_python_scripts_dir` and `file_paths.api_calls_dir` may be vestigial — the Python executor ignores `executed_python_scripts_dir` and uses its own workspace-relative paths.

**Approach**:
1. Remove `simulate_delays` from config template (or implement it in mock connector)
2. Audit `file_paths` keys for runtime consumption, remove unused ones

**Status**: pending

---

### [P3] GP-027: Undocumented config keys read by code (pending)

**Severity**: UX | **Categories**: config_flexibility
**Files**: `src/osprey/templates/project/config.yml.j2`, `src/osprey/templates/claude_code/claude/hooks/osprey_hook_log.py`, `src/osprey/registry/web.py`
**Affects**: All facilities (discoverability)

**Problem**: Several config keys are read by code but absent from `config.yml.j2`:
- `hooks.debug` (hook logging utility, line 105)
- `artifact_server.host/port/auto_launch` (approval hook, web registry)
- Web server ports for tuning, deplot, channel_finder, lattice_dashboard (ports 8085-8097)

**Approach**:
1. Add commented-out sections for each undocumented key with defaults
2. Group companion web servers in a reference section

**Status**: pending

---

### [P3] GP-028: ALS-specific content in lattice templates (pending)

**Severity**: UX | **Categories**: prompt_customizability, portability
**Files**: `src/osprey/templates/claude_code/claude/rules/lattice-physics.md.j2`, `src/osprey/templates/claude_code/CLAUDE.md.j2`, `src/osprey/templates/claude_code/claude/skills/lattice-evaluation/references/*.py`
**Affects**: Non-ALS facilities using lattice_design template

**Problem**: Lattice templates contain ALS-specific content: `ALS Working Point` section with tune values `nu_x ~ 14.25, nu_y ~ 8.18`, hardcoded `machine_data/als.m` file path, `ALS Frequency Map Analysis` plot titles. Non-ALS facilities using lattice_design get incorrect reference values and ALS-branded outputs.

**Approach**:
1. Template tune values and lattice file path with config-driven variables
2. Replace `ALS` with `{{ facility_name }}` in plot titles
3. Source working point data from lattice file metadata

**Status**: pending
