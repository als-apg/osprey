# Prometheus configuration — generated by OSPREY
# Do not edit; re-generated on each launch.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["{{ otel_host }}:{{ otel_prometheus_port }}"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["{{ prometheus_host }}:{{ prometheus_port }}"]
