"""Backward-compatibility shim — moved to osprey.connectors.control_system.limits_validator."""

from osprey.connectors.control_system.limits_validator import (  # noqa: F401
    DEFAULTS_FIELD,
    METADATA_FIELDS,
    ChannelLimitsConfig,
    LimitsValidator,
)
