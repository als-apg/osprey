"""DePlot service — graph data extraction via Google's DePlot model.

Standalone FastAPI service that accepts chart images and returns extracted
data tables. Start with:

    uv run python -m osprey.services.deplot
"""
