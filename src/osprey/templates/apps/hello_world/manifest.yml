name: hello_world
description: "Minimal hello world — one MCP server, mock control system"

artifacts:
  hooks:
    - hook-log
    - hook-config
    - error-guidance
  rules:
    - safety
    - error-handling
    - facility
    - timezone
  skills: []
  agents: []
  output_styles:
    - control-operator

web_panels: []
