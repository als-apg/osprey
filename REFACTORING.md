# Refactoring Backlog

> Auto-generated by `/refactor scan` on 2026-03-03.
> Use `/refactor next` to execute items. Use `/refactor status` to check progress.

## Summary

- Total items: 31
- Completed: 2
- Remaining: 29
- High priority (P0): 4 | Medium-high (P1): 11 | Medium (P2): 10 | Low (P3): 6

## Backlog

### [P0] RF-001: hierarchical.py is a 2086-line god-module (skipped)

**Severity**: high | **Category**: structure, cohesion
**Files**: `src/osprey/services/channel_finder/databases/hierarchical.py`
**Impact radius**: 12 files import from this module

**Problem**: The largest file in the codebase — a single `HierarchicalChannelDatabase` class spanning 2086 lines. Tree navigation, channel expansion, data loading, validation, and query operations are all conflated in one class. Part of the broader Channel Finder scatter problem (RF-013).

**Approach**:
1. Extract tree navigation methods into `tree_navigation.py`
2. Extract channel expansion/pattern logic into `channel_expansion.py`
3. Extract validation into `validation.py`
4. Keep `HierarchicalChannelDatabase` as a facade delegating to these modules

**Status**: skipped
**Completed date**: 2026-03-03
**Notes**: Owner prefers the current single-class structure.

---

### [P0] RF-002: utils/config.py is a 787-line coupling magnet with 35 importers and overlapping APIs (completed)

**Severity**: high | **Category**: structure, dependency, cohesion
**Files**: `src/osprey/utils/config.py`, `src/osprey/utils/config_writer.py`
**Impact radius**: 35 files import config.py; config_writer.py has overlapping read-only helpers

**Problem**: Three agents converge on this file. **Structure**: 787 lines with ConfigBuilder (300+ lines), 14 convenience functions, env-var resolution, and model config helpers. **Dependency**: 35 importers across 9 architectural layers — the third-highest fan-in in the codebase. **Cohesion**: config_writer.py duplicates read-only helpers (config_read, get_control_system_type, get_epics_gateway_config, get_all_model_configs, find_config_file) using ruamel.yaml instead of the ConfigBuilder singleton, creating two parallel config reading paths with different parsers and no shared state. VALID_TIERS and resolve_model_id() are model domain knowledge misplaced in a generic config utility.

**Approach**:
1. ~~Move read-only helpers from config_writer.py into config.py~~ (deferred — read-only helpers implement a read-before-write pattern co-imported with mutation functions)
2. ~~Restrict config_writer.py to mutation operations only~~ (deferred — same reason)
3. Move VALID_TIERS and resolve_model_id() to `models/tiers.py`
4. Move model-specific helpers (get_model_config, get_provider_config) to `models/config.py`

**Status**: completed
**Completed date**: 2026-03-03
**Notes**: Scope reduced to model-domain extraction only. Moved 4 symbols (VALID_TIERS, resolve_model_id, get_model_config, get_provider_config) to models/tiers.py and models/config.py. Updated 9 source files and 2 test files. config_writer.py consolidation deferred.

---

### [P0] RF-003: server_catalog.py — misplaced domain logic, legacy shims, misleading name (done)

**Severity**: high | **Category**: structure, naming, cohesion, vestige
**Files**: `src/osprey/registry/mcp.py` (was `src/osprey/cli/server_catalog.py`)
**Impact radius**: 6 files import; blocks template refactoring

**Problem**: Four agents flag this file — the strongest convergence signal in the scan. **Structure**: 646 lines. **Naming**: "catalog" suggests a browsable interface, but this is the authoritative source of MCP server/agent metadata (ServerDefinition, AgentDefinition, FRAMEWORK_SERVERS, FRAMEWORK_AGENTS). **Cohesion**: Domain logic (server registry) lives in the CLI presentation layer. **Vestige**: Contains backward-compatibility shims for deprecated config keys (disable_servers, disable_agents, extra_servers) including _custom_server_from_legacy() — the v0.11 migration already converts these.

**Approach**:
1. Move to `registry/server_definitions.py` or `infrastructure/server_registry.py`
2. Rename to reflect its actual purpose (authoritative server/agent metadata)
3. Remove legacy config shims (disable_servers, disable_agents, extra_servers) after confirming migration is complete
4. Update template layer imports

**Status**: done
**Completed date**: 2026-03-03
**Notes**: Moved `cli/server_catalog.py` → `registry/mcp.py`. Name follows registry layer's single-word convention and is harness-agnostic. Legacy shim removal deferred to RF-027.

---

### [P0] RF-004: server_launcher.py inverts dependency direction with 6 layering violations (done)

**Severity**: high | **Category**: dependency, cohesion
**Files**: `src/osprey/infrastructure/server_launcher.py`
**Impact radius**: 8 files reference; all interface app factories affected

**Problem**: Infrastructure layer (level 7) directly imports 6 interface/service app factories: `interfaces.artifacts.app.create_app`, `interfaces.ariel.app.create_app`, `interfaces.tuning.app.create_app`, `services.deplot.server.create_app`, `interfaces.channel_finder.app.create_app`, `interfaces.lattice_dashboard.app.create_app`. This inverts the dependency direction — infrastructure should not know about specific interfaces. The stores/artifact_store.py also imports from server_launcher (ensure_artifact_server), creating a circular concern.

**Approach**:
1. Use a registration pattern: interfaces register their `create_app` factories with the launcher
2. Or reclassify server_launcher as part of the CLI layer (since it orchestrates app startup)
3. Decouple artifact_store from server_launcher via initialization hooks

**Status**: done
**Completed date**: 2026-03-03
**Notes**: Created `registry/web.py` with `WebServerDefinition` catalog (6 entries). Replaced ~200 lines of per-server boilerplate in `server_launcher.py` with ~40 lines of generic code using `importlib` dynamic loading. All 6 `ensure_*` functions preserved as backward-compat aliases. Zero callers needed changes.

---

### [P1] RF-005: cli/ariel.py is a 1059-line god-module with 12 fan-out imports (pending)

**Severity**: high | **Category**: structure, dependency
**Files**: `src/osprey/cli/ariel.py`
**Impact radius**: 12 osprey modules across 4 layers

**Problem**: 12 Click command handlers with substantial inline business logic. The CLI module directly orchestrates 12 modules spanning interfaces, models, services, and utils layers. Each command (status, migrate, ingest, watch, enhance, models, search, reembed) contains 50-100 lines of orchestration that should live in the services layer.

**Approach**:
1. Extract each command's business logic into service functions in `services/ariel_search/`
2. CLI handlers become thin wrappers: parse args, call service, format output
3. Consider splitting CLI into `ariel_data_cmds.py` and `ariel_query_cmds.py`

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-006: compose_generator.py is a 985-line god-module (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/deployment/compose_generator.py`
**Impact radius**: 5 files

**Problem**: 985 lines of procedural code with 12 top-level functions mixing template rendering, file copying, config injection, build directory management, and compose file orchestration. Functions like `setup_build_dir` span hundreds of lines.

**Approach**:
1. Split into `template_renderer.py` (Jinja2 ops)
2. Extract `build_dir_manager.py` (directory setup, file copying, agent data)
3. Keep `compose_orchestrator.py` (top-level compose generation)

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-007: container_engine.py is 958 lines with 6 classes (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/services/python_executor/execution/container_engine.py`
**Impact radius**: 4 files

**Problem**: Six classes (ContainerEndpoint, SessionInfo, JupyterSessionManager, CodeExecutionEngine, FileBasedResultCollector, ContainerExecutor) plus a top-level async function. These are distinct concerns: container lifecycle, Jupyter session management, code execution, and result collection.

**Approach**:
1. Extract `session_manager.py` (JupyterSessionManager, SessionInfo)
2. Extract `result_collector.py` (FileBasedResultCollector)
3. Extract `execution_engine.py` (CodeExecutionEngine)
4. Keep `container_executor.py` (ContainerExecutor, ContainerEndpoint)

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-008: health_cmd.py is a 957-line god-module with duplicated dotenv logic (pending)

**Severity**: high | **Category**: structure, cohesion
**Files**: `src/osprey/cli/health_cmd.py`
**Impact radius**: 3 files

**Problem**: Contains HealthCheckResult, HealthChecker (with checks for config, connectors, MCP servers, containers, models), and CLI handler. Mixes check logic with rendering. Duplicates dotenv loading that also exists in mcp_env.py and utils/config.py (see RF-016).

**Approach**:
1. Extract HealthChecker into `services/health/` as a standalone service
2. Split into pluggable checkers: ConfigChecker, ConnectorChecker, McpServerChecker, ContainerChecker
3. Keep health_cmd.py as thin CLI wrapper

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-009: database_api.py is 901 lines with 12 models + 31 endpoints for 3 pipelines (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/interfaces/channel_finder/database_api.py`
**Impact radius**: 4 files

**Problem**: A single FastAPI router with 12 Pydantic request models and 31 route handlers for three pipeline types (hierarchical, middle_layer, in_context) all mixed together.

**Approach**:
1. Split into `hierarchical_routes.py`, `middle_layer_routes.py`, `in_context_routes.py`
2. Move request models to `schemas.py`
3. Use APIRouter includes to compose in database_api.py

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-010: preview_database.py is 902 lines with 20 functions for 3 pipelines (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/services/channel_finder/tools/preview_database.py`
**Impact radius**: 5 files

**Problem**: 20 top-level functions implementing preview rendering for three pipeline types. Each pipeline has its own set of helpers but they share a single file with no clear boundaries.

**Approach**:
1. Split into `preview_middle_layer.py`, `preview_hierarchical.py`, `preview_in_context.py`
2. Keep preview_database.py as a thin dispatcher

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-011: project_actions.py is 824 lines with 11 fan-out imports (pending)

**Severity**: high | **Category**: structure, dependency
**Files**: `src/osprey/cli/project_actions.py`
**Impact radius**: 11 modules across 3 layers

**Problem**: 11 handler functions for the interactive TUI menu covering deploy, health, tasks, export, and configuration management. Each handler is 50-100+ lines. Mixes concerns from different CLI domains.

**Approach**:
1. Split into `deploy_actions.py`, `config_actions.py`, `task_actions.py`
2. Each handler should delegate to the corresponding service layer

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-012: ARIEL code scattered across 3 packages with naming conflicts (pending)

**Severity**: medium | **Category**: cohesion, naming
**Files**: `src/osprey/services/ariel_search/`, `src/osprey/mcp_server/ariel/`, `src/osprey/interfaces/ariel/`, `src/osprey/services/ariel_search/capability.py`, `src/osprey/services/ariel_search/capabilities.py`
**Impact radius**: 40+ files in the ARIEL subsystem

**Problem**: ARIEL code lives in three packages (services, mcp_server, interfaces) with duplicated config parsing between `mcp_server/ariel/server_context.py` and `services/ariel_search/config.py`. Additionally: `capability.py` vs `capabilities.py` near-collision in the same directory (factory vs frontend capabilities), `ConfigurationError` defined 3 times (errors.py, ariel exceptions, channel_finder exceptions), and `ErrorCategory` enum defined twice with different values (ariel vs python_executor).

**Approach**:
1. Move converter logic from `mcp_server/ariel/converters.py` to `services/ariel_search/`
2. Have server_context delegate config parsing to `services/ariel_search/config.py`
3. Rename `capability.py` → `service_factory.py`
4. Rename service-specific exceptions: `ARIELConfigurationError`, `ChannelFinderConfigurationError`, `ARIELErrorCategory`, `ExecutorErrorCategory`

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-013: Channel Finder has 4 duplicated MCP server packages with naming issues (pending)

**Severity**: medium | **Category**: cohesion, naming
**Files**: `src/osprey/mcp_server/channel_finder_hierarchical/`, `src/osprey/mcp_server/channel_finder_in_context/`, `src/osprey/mcp_server/channel_finder_middle_layer/`, `src/osprey/mcp_server/direct_channel_finder/`
**Impact radius**: 20+ files

**Problem**: Four MCP server packages follow identical structural patterns (server.py, server_context.py, tools/__init__.py, __main__.py) with duplicated initialization logic. Each independently loads config.yml and creates database instances. Additional naming issues: `ChannelDatabase` class name reused in flat.py and template.py; `backends/` naming confuses with `connectors/`; `template.py` means "parameterized patterns" not Jinja2.

**Approach**:
1. Extract shared MCP server init into a common factory in `services/channel_finder/`
2. Consider consolidating into a single `mcp_server/channel_finder/` with dynamic tool selection
3. Rename classes: `FlatChannelDatabase`, `ParameterizedChannelDatabase` at definition site
4. Rename `backends/` → `pv_sources/` or `live_providers/`

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-014: mcp_server/export/ — misleading name and misplaced utility (pending)

**Severity**: medium | **Category**: naming, cohesion
**Files**: `src/osprey/mcp_server/export/converter.py`
**Impact radius**: 4 files (ariel converters, artifact_export, 2 tests)

**Problem**: `mcp_server/export/` sounds like it exports MCP server definitions, but actually provides `convert_html_to_image()` using Playwright — a general-purpose utility. Used by both ARIEL and workspace MCP tools. Contains no MCP-specific logic.

**Approach**:
1. Move to `utils/html_renderer.py` or `infrastructure/export/`
2. Rename package/module to reflect actual purpose (HTML-to-image conversion)

**Status**: pending
**Completed date**:
**Notes**:

---

### [P1] RF-015: services/migration/ — 'migration' means two different things (pending)

**Severity**: medium | **Category**: naming, structure
**Files**: `src/osprey/services/migration/`, `src/osprey/services/ariel_search/database/migrations.py`
**Impact radius**: 8 files

**Problem**: `services/migration/` handles OSPREY project template upgrades (three-way diffs). `services/ariel_search/database/migrations.py` handles PostgreSQL schema migrations (CREATE TABLE, ALTER TABLE). Completely different concepts sharing the word "migration." Additionally, services/migration/ is a single-implementation-file package (__init__.py re-exports from engine.py).

**Approach**:
1. Rename `services/migration/` → `services/project_upgrade/` (or `template_sync/`)
2. CLI command can remain `osprey migrate` for user familiarity
3. Flatten the unnecessary package wrapper (remove __init__.py re-exports)

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-016: Dotenv loading duplicated 3 times + env-var resolution duplicated (pending)

**Severity**: medium | **Category**: cohesion
**Files**: `src/osprey/mcp_env.py`, `src/osprey/utils/config.py`, `src/osprey/cli/health_cmd.py`, `src/osprey/mcp_server/introspect.py`
**Impact radius**: 10 files

**Problem**: Dotenv loading implemented three separate times with slightly different resolution strategies. Additionally, `${VAR:-default}` env-var resolution exists in both `utils/config.py` (resolve_env_vars) and `mcp_server/introspect.py` (_resolve_env_value) with different capabilities and edge-case behavior.

**Approach**:
1. Consolidate dotenv loading into a single function in `utils/workspace.py` or `utils/config.py`
2. Remove `mcp_env.py` (orphan at package root)
3. Have `introspect.py` delegate to `resolve_env_vars` from `utils/config.py`

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-017: services/prompts/ownership.py layering violation (services → cli) (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/services/prompts/ownership.py`
**Impact radius**: 3 files

**Problem**: Business logic in services/ imports from CLI layer: `from osprey.cli.templates import manifest` and `from osprey.cli.templates.manifest import MANIFEST_FILENAME`. Services layer should not depend on CLI.

**Approach**:
1. Move manifest metadata (MANIFEST_FILENAME, manifest reading) to a shared location in `services/prompts/` or `utils/`
2. CLI templates should import from services, not vice versa

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-018: interfaces/web_terminal layering violation (interfaces → cli) (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/interfaces/web_terminal/prompt_gallery_service.py`
**Impact radius**: 5 files

**Problem**: Web terminal's prompt gallery reaches into CLI layer: imports from `osprey.cli.templates.manager` and `osprey.cli.templates.claude_code`. Interfaces should not depend on CLI.

**Approach**:
1. Extract template management into a `services/` module
2. Both CLI and interfaces import from the shared service

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-019: logbook.py has domain logic in the interface layer (pending)

**Severity**: medium | **Category**: cohesion
**Files**: `src/osprey/interfaces/artifacts/logbook.py`
**Impact radius**: 1 file (app.py)

**Problem**: ~400 lines containing LLM prompt assembly (PURPOSE_FRAGMENTS, DETAIL_FRAGMENTS, assemble_prompt()), Claude API calls for narrative composition, ARIEL draft submission, and session metadata gathering. This is business logic masquerading as a FastAPI router. Imports from 3 different layers (mcp_server, utils, services).

**Approach**:
1. Extract logbook composition logic into `services/ariel_search/logbook_composer.py`
2. Reduce logbook.py to a thin FastAPI router delegating to the service

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-020: runtime/ — single-file package + layering violation + naming collision (pending)

**Severity**: medium | **Category**: structure, dependency, naming
**Files**: `src/osprey/runtime/__init__.py`, `src/osprey/deployment/runtime_helper.py`
**Impact radius**: 4 files

**Problem**: Three issues converge. **Structure**: runtime/ is a 260-line `__init__.py` with no submodules — a directory masquerading as a module. **Dependency**: imports from connectors (layer 5), violating layering for a level-7 primitive. **Naming**: "runtime" means both "control system channel read/write API" (runtime/) and "Docker/Podman container detection" (deployment/runtime_helper.py).

**Approach**:
1. Convert runtime/ package → `runtime.py` plain module
2. Rename `deployment/runtime_helper.py` → `deployment/container_runtime.py`
3. Accept connector as parameter to write_channel/read_channel instead of importing directly

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-021: 8 medium god-modules (650-802 lines each) (pending)

**Severity**: medium | **Category**: structure
**Files**: `src/osprey/services/channel_finder/databases/middle_layer.py` (802), `src/osprey/mcp_server/workspace/transcript_reader.py` (790), `src/osprey/cli/interactive_menu.py` (783), `src/osprey/cli/init_wizard.py` (776), `src/osprey/cli/claude_cmd.py` (767), `src/osprey/interfaces/artifacts/app.py` (765), `src/osprey/cli/migrate_cmd.py` (671), `src/osprey/mcp_server/workspace/execution/sandbox_executor.py` (669)
**Impact radius**: varies per file

**Problem**: Eight files in the 650-802 line range, each mixing concerns that could be separated. These are lower priority than the 900+ line god-modules but represent ongoing maintenance burden.

**Approach**: Apply same splitting patterns as their larger counterparts — extract business logic from CLI handlers, split multi-concern classes, separate rendering from logic.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-022: High fan-in coupling magnets: logger (50), mcp_server/errors (36), workspace (35) (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/utils/logger.py`, `src/osprey/mcp_server/errors.py`, `src/osprey/utils/workspace.py`
**Impact radius**: 50 + 36 + 35 = 121 importers

**Problem**: Three modules with 35+ importers each. `logger.py` (50 importers) wraps stdlib logging — any API change cascades everywhere. `mcp_server/errors.py` (36 importers) serves all 5 MCP server packages from a single file. `workspace.py` (35 importers) provides path resolution used across most MCP servers and web interfaces.

**Approach**:
1. Consider using stdlib logging directly instead of custom wrapper
2. Split mcp_server/errors.py into per-server error modules with shared base
3. Consider injecting workspace paths via constructor parameters

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-023: Misplaced cross-cutting utilities: session.py, http.py, mcp_env.py (pending)

**Severity**: low | **Category**: cohesion
**Files**: `src/osprey/mcp_server/session.py`, `src/osprey/mcp_server/http.py`, `src/osprey/mcp_env.py`
**Impact radius**: 6 files

**Problem**: Three utility modules live under mcp_server/ but contain no MCP-specific logic and are imported by interfaces/. `session.py` gathers runtime metadata (git, transcript, operator). `http.py` provides URL construction and fire-and-forget HTTP POST. `mcp_env.py` is an orphan at the package root for dotenv loading. All create cross-layer dependencies.

**Approach**: Move to `utils/` or `infrastructure/` to eliminate cross-layer imports.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-024: facility_presets.py layering violation (utils → templates) (pending)

**Severity**: low | **Category**: cohesion
**Files**: `src/osprey/templates/data/facility_presets.py`
**Impact radius**: 2 files (config_writer.py, init_wizard.py)

**Problem**: FACILITY_PRESETS (EPICS gateway configs for APS, ALS, simulation) lives in `templates/data/` but is imported by `utils/config_writer.py`. The templates/ package is for scaffolding templates, not runtime configuration data. This creates a layering violation where utils depends on templates.

**Approach**: Move `facility_presets.py` to `utils/` or a config package.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P2] RF-025: DePlot server lives in services/ instead of interfaces/ (pending)

**Severity**: low | **Category**: cohesion
**Files**: `src/osprey/services/deplot/server.py`
**Impact radius**: 1 file (server_launcher.py)

**Problem**: DePlot bundles its FastAPI server (with /health and /extract endpoints) alongside domain logic (model.py, preprocessing.py). Other services separate web servers into interfaces/ (e.g., interfaces/ariel/app.py wraps services/ariel_search/). Inconsistent pattern.

**Approach**: Move `services/deplot/server.py` → `interfaces/deplot/app.py`.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-026: 47 documentation files reference removed LangGraph architecture (pending)

**Severity**: low | **Category**: vestige
**Files**: `docs/source/developer-guides/01_understanding-the-framework/03_langgraph-integration.rst`, `docs/source/index.rst`, `docs/source/getting-started/conceptual-tutorial.rst`, + 44 more
**Impact radius**: Documentation only

**Problem**: 47 documentation files still describe Osprey as "Built on LangGraph's StateGraph foundation." An entire dedicated page describes LangGraph integration. conf.py still mocks langgraph imports. Zero LangGraph imports exist in src/osprey/.

**Approach**: Remove 03_langgraph-integration.rst. Rewrite docs to describe Claude Code MCP architecture. Remove langgraph from conf.py autodoc_mock_imports.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-027: Legacy Jinja2 template guards + dead wrapper code (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/templates/claude_code/claude/agents/*.md.j2`, `src/osprey/services/python_executor/execution/wrapper.py`, `src/osprey/services/python_executor/execution/control.py`
**Impact radius**: 14 files

**Problem**: 12 Jinja2 templates use `{% if 'name' not in disable_servers %}` guards — backward-compat shims for the old config format. `wrapper.py` has a dead `_get_context_loading()` method setting `context = None` for the deleted osprey.context module. `control.py` docstring references deleted modules (policy_analyzer, analysis.node).

**Approach**:
1. Refactor templates to use new-format context variables (enabled flags)
2. Remove `_get_context_loading()` from wrapper.py
3. Clean up stale docstring references

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-028: 6 too-small packages that should be plain modules (pending)

**Severity**: low | **Category**: structure
**Files**: `src/osprey/interfaces/agentsview/` (140 lines), `src/osprey/interfaces/tuning/` (158), `src/osprey/mcp_server/export/` (116), `src/osprey/services/ariel_search/prompts/` (36), `src/osprey/infrastructure/` (326), `src/osprey/services/ariel_search/agent/` (493)
**Impact radius**: 2-4 files each

**Problem**: Six packages with only __init__.py + one implementation file, where the package wrapper adds no value. Unnecessary directory nesting and indirection.

**Approach**: Convert to plain modules where no submodule expansion is planned. Some overlap with RF-014 (export/) and RF-020 (runtime/).

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-029: Stale docstrings and references to deleted modules (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/models/logging.py`, `src/osprey/models/embeddings/base.py`, `src/osprey/models/embeddings/ollama.py`, `src/osprey/services/ariel_search/database/migrations.py`, `src/osprey/stores/artifact_store.py`
**Impact radius**: Documentation only

**Problem**: Docstrings reference ClassificationNode/CapabilityClassifier (deleted LangGraph components), non-existent spec files (01_DATA_LAYER.md Section 6.3.1), and internal issue-tracking documents. artifact_store.py has a migration shim for the removed "highlighted" concept.

**Approach**: Update docstring examples to reference current callers. Remove stale spec references. Remove "highlighted" shim after confirming no old workspaces remain.

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-030: Database error classes — inconsistent naming and placement (pending)

**Severity**: low | **Category**: naming
**Files**: `src/osprey/services/channel_finder/core/base_database.py`, `src/osprey/services/channel_finder/core/exceptions.py`, `src/osprey/services/ariel_search/exceptions.py`
**Impact radius**: 8 files

**Problem**: DatabaseWriteError lives in base_database.py, not in exceptions.py alongside sibling DatabaseLoadError. ARIEL database exceptions (DatabaseConnectionError, DatabaseQueryError) use generic "Database" prefix without service qualification.

**Approach**:
1. Move DatabaseWriteError to channel_finder/core/exceptions.py
2. Consider prefixing ARIEL exceptions: ARIELDatabaseConnectionError, ARIELDatabaseQueryError

**Status**: pending
**Completed date**:
**Notes**:

---

### [P3] RF-031: Naming concept collisions across 4 terms (pending)

**Severity**: low | **Category**: naming
**Files**: various (see description)
**Impact radius**: Low — naming-only changes

**Problem**: Four terms are overloaded across the codebase:
- **"templates"**: project scaffolding (templates/), Jinja2 rendering (cli/templates/), parameterized channel patterns (databases/template.py)
- **"prompts"**: Claude Code artifact management (services/prompts/), LLM prompt text (ariel_search/prompts/)
- **"execution"**: sandbox visualization executor (workspace/execution/), full container engine (python_executor/execution/)
- **"backends"**: PV metadata lookup (channel_finder/backends/), conceptually similar to connectors/

**Approach**:
1. Rename `databases/template.py` → `databases/parameterized.py`
2. Rename `services/prompts/` → `services/prompt_artifacts/` or `services/claude_artifacts/`
3. Rename `workspace/execution/` → `workspace/sandbox/`
4. Rename `channel_finder/backends/` → `channel_finder/pv_sources/`

**Status**: pending
**Completed date**:
**Notes**:
