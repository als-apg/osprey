# Refactoring Backlog

> Auto-generated by `/refactor scan` on 2026-03-02.
> Use `/refactor next` to execute items. Use `/refactor status` to check progress.

## Summary

- Total items: 34
- Completed: 1
- Remaining: 33
- High priority (P0): 3 | P1: 9 | P2: 12 | P3: 10

---

## Backlog

### [P0] RF-001: web_terminal/routes.py — 1246-line god-module with 13 cross-layer imports (completed)

**Severity**: high | **Category**: structure, dependency
**Files**: `src/osprey/interfaces/web_terminal/routes.py`
**Impact radius**: ~15 files (routes + MCP imports + test files)

**Problem**: 58 route handler functions in a single file covering HTTP endpoints, WebSocket handlers, SSE routes, memory management, prompt gallery, and session management. Additionally imports from 13 MCP server internals (artifact_store, introspect, type_registry, workspace tools, transcript_reader), creating tight cross-layer coupling where the interfaces layer depends on MCP implementation details.

**Approach**:
1. Split routes into domain-specific modules: `routes/session.py`, `routes/memory.py`, `routes/prompts.py`, `routes/config.py`, `routes/websocket.py`
2. Create a session/workspace service in `osprey.services/` that wraps transcript reading, session summaries, and artifact access
3. Replace direct MCP tool imports with service facade calls

**Status**: completed
**Completed date**: 2026-03-02
**Notes**: Split into 7 sub-modules (panels, session, config, files, memory, prompts, websocket) with composite router in `__init__.py`. Service facade (step 2-3) deferred. MCP imports remain as lazy imports within function bodies. All 3686 tests pass.

---

### [P0] RF-002: channel_finder/database_api.py — 994-line handler with 14 cross-layer MCP imports (pending)

**Severity**: high | **Category**: structure, dependency
**Files**: `src/osprey/interfaces/channel_finder/database_api.py`
**Impact radius**: ~20 files (imports from 14 MCP modules)

**Problem**: This interface-layer file directly imports from 14 distinct `mcp_server` modules — registries, tools, and servers for all channel finder variants. It calls MCP tool functions directly rather than going through a service layer, creating the highest fan-out file in the codebase. This tightly couples the web UI to MCP server internals.

**Approach**:
1. Create a service-layer facade (e.g., `osprey.services.channel_finder.api/`) that wraps the MCP tool functions
2. Have the interface layer call the service facade
3. This also enables testing the web API without spinning up MCP servers

**Status**: pending
**Notes**:

---

### [P0] RF-003: utils/config.py cluster — 787-line god-module + 3 competing config systems (pending)

**Severity**: high | **Category**: structure, dependency, cohesion
**Files**: `src/osprey/utils/config.py`, `src/osprey/utils/config_updater.py`, `src/osprey/utils/yaml_config.py`, `src/osprey/utils/workspace.py`
**Impact radius**: ~40 files (34 config.py importers + config_updater/yaml_config callers)

**Problem**: Three config-related issues converge:
- **Structure**: `config.py` is 787 lines with 33 functions covering YAML loading, env-var resolution, dot-path access, validation, merging, and tier definitions
- **Dependency**: 34 importers across every layer — changes have maximum blast radius
- **Cohesion**: Three competing config mutation systems: `config_updater.py` and `yaml_config.py` both provide comment-preserving YAML updates with overlapping APIs; `workspace.py` has its own config cache independent of `ConfigBuilder`, risking cache inconsistency; MCP server registries have custom `_load_config()` methods bypassing the canonical system

**Approach**:
1. Split `config.py` into `config_reader.py` (loading, env resolution), `config_access.py` (dot-path, merging), `config_validation.py` (schema/tier)
2. Consolidate `config_updater.py` and `yaml_config.py` into a single `config_writer.py`
3. Standardize MCP server registries to use `ConfigBuilder` instead of custom loaders
4. Unify the `workspace.py` config cache with `ConfigBuilder`

**Status**: pending
**Notes**:

---

### [P1] RF-004: cli/templates.py — 1973-line god-module (pending)

**Severity**: high | **Category**: structure, naming
**Files**: `src/osprey/cli/templates.py`
**Impact radius**: ~10 files

**Problem**: `TemplateManager` handles template discovery, Jinja2 rendering, project scaffolding, service config copying, manifest generation, and prompt registry integration — all in 1973 lines. Additionally, the module name `templates.py` shadows the `templates/` package directory, creating ambiguity between the template engine and the template content.

**Approach**: Split into a `cli/templates/` package with `discovery.py`, `renderer.py`, `scaffolding.py`, and `manifest.py`. This also resolves the naming collision.

**Status**: pending
**Notes**:

---

### [P1] RF-005: Store infrastructure misplaced in mcp_server/ (pending)

**Severity**: high | **Category**: dependency, cohesion
**Files**: `src/osprey/mcp_server/artifact_store.py`, `src/osprey/mcp_server/base_store.py`, `src/osprey/mcp_server/type_registry.py`, `src/osprey/mcp_server/notebook_renderer.py`
**Impact radius**: ~17 files (10 MCP importers + 2 interface importers + tests)

**Problem**: `ArtifactStore`, `BaseStore`, `type_registry`, and `notebook_renderer` are not MCP-specific — they are cross-cutting workspace/storage infrastructure. They are imported by `interfaces/artifacts/app.py` and `interfaces/web_terminal/routes.py`, creating a layering violation where the interfaces layer depends on MCP internals.

**Approach**: Move to a new `src/osprey/stores/` package. Both `mcp_server/` and `interfaces/` import from this shared layer instead.

**Status**: pending
**Notes**:

---

### [P1] RF-006: python_executor/models.py — 1078 lines with dead LangGraph models (pending)

**Severity**: high | **Category**: structure, vestige
**Files**: `src/osprey/services/python_executor/models.py`
**Impact radius**: ~8 files

**Problem**: Contains 21 classes in 1078 lines mixing active execution models with dead LangGraph vestiges. `PlanningMode`, `AnalysisPhase`, `StructuredPlan` are never used outside this file. Three `get_*_from_configurable()` functions (lines 806, 867, 934) parse the LangGraph configurable dict pattern but are never called. Docstrings reference deleted classes (`PythonExecutorService`, `PythonExecutionState`).

**Approach**:
1. Remove dead classes: `PlanningMode`, `AnalysisPhase`, `StructuredPlan`
2. Remove dead fields: `planning_mode`, `structured_plan` from `PythonExecutionRequest`
3. Remove dead functions: `get_execution_control_config_from_configurable()`, `get_execution_mode_config_from_configurable()`, `get_container_endpoint_config_from_configurable()`
4. Fix stale docstring references
5. Split remaining models into domain-specific files if still >500 lines

**Status**: pending
**Notes**:

---

### [P1] RF-007: "registry" concept collision across 10+ modules (pending)

**Severity**: high | **Category**: naming, cohesion
**Files**: `src/osprey/registry/manager.py`, `src/osprey/mcp_server/*/registry.py` (6 files), `src/osprey/cli/prompt_registry.py`, `src/osprey/cli/server_registry.py`, `src/osprey/models/provider_registry.py`
**Impact radius**: ~30 files

**Problem**: "Registry" means three fundamentally different things: (1) Framework component registry (`RegistryManager`) — dependency injection container, (2) MCP server state singletons (6 `registry.py` files) — service locator pattern, (3) CLI catalogs (`PromptRegistry`, `ServerRegistry`) — static artifact lists. Having 10+ classes all named `*Registry` makes navigation confusing. Additionally, `registry/registry.py` creates the redundant import path `osprey.registry.registry`.

**Approach**:
1. Rename MCP server `registry.py` files to `server_state.py` (e.g., `ARIELMCPState`)
2. Rename CLI catalogs to `prompt_catalog.py`, `server_catalog.py`
3. Rename `registry/registry.py` to `registry/builtins.py`
4. Reserve "registry" exclusively for `osprey.registry.RegistryManager`

**Status**: pending
**Notes**:

---

### [P1] RF-008: Duplicate YAML mutation systems (pending)

**Severity**: high | **Category**: naming, cohesion
**Files**: `src/osprey/utils/config_updater.py`, `src/osprey/utils/yaml_config.py`
**Impact radius**: ~8 files

**Problem**: Two modules provide overlapping comment-preserving YAML update functionality using `ruamel.yaml`. `yaml_config.py` claims to be "single source of truth for config modifications" but `config_updater.py` is used by 6 callers. `config_updater.py` also duplicates config reading logic (`find_config_file`, `get_control_system_type`) that overlaps with `config.py`.

**Approach**: Consolidate into a single `config_writer.py`. Move domain-specific functions from `config_updater.py` to CLI-level code. Delete `config_updater.py`.

**Status**: pending
**Notes**: Overlaps with RF-003. Execute RF-003 first.

---

### [P1] RF-009: prompt_gallery_service.py imports from cli/ — layering violation (pending)

**Severity**: high | **Category**: dependency
**Files**: `src/osprey/interfaces/web_terminal/prompt_gallery_service.py`
**Impact radius**: ~5 files

**Problem**: The interfaces layer (L2) imports from the cli layer (L1 — highest): `PromptArtifact`, `PromptRegistry` from `cli.prompt_registry`, multiple functions from `cli.prompts_cmd`, and `TemplateManager` from `cli.templates`. This creates a circular architectural dependency.

**Approach**: Extract `PromptRegistry`, `PromptArtifact`, and template access into a shared services module (e.g., `osprey.services.prompts/`). CLI commands become thin wrappers delegating to the shared service.

**Status**: pending
**Notes**:

---

### [P1] RF-010: services/channel_finder/tools/ imports from cli.styles — layering violation (pending)

**Severity**: high | **Category**: dependency
**Files**: `src/osprey/services/channel_finder/tools/preview_database.py`, `src/osprey/services/channel_finder/tools/validate_database.py`
**Impact radius**: ~4 files

**Problem**: Two service-layer files (L4) import `console` and `get_active_theme` from `osprey.cli.styles` (L1). Service code should not depend on CLI presentation concerns.

**Approach**: Move shared console/theme utilities to `osprey.utils.output` or inject an output abstraction into these tools.

**Status**: pending
**Notes**:

---

### [P1] RF-011: ConfigurationError defined in 3 modules with same name (pending)

**Severity**: medium | **Category**: naming, cohesion
**Files**: `src/osprey/errors.py`, `src/osprey/services/ariel_search/exceptions.py`, `src/osprey/services/channel_finder/core/exceptions.py`
**Impact radius**: ~8 files

**Problem**: Three `ConfigurationError` classes with different inheritance hierarchies and constructor signatures. A bare `except ConfigurationError` will silently miss the other two variants.

**Approach**: Make service-specific exceptions inherit from the framework-level `osprey.errors.ConfigurationError` for consistent catch-all handling, while keeping their service-specific constructors.

**Status**: pending
**Notes**:

---

### [P1] RF-012: migrate.py vs migration.py near-collision (pending)

**Severity**: medium | **Category**: naming
**Files**: `src/osprey/services/ariel_search/database/migrate.py`, `src/osprey/services/ariel_search/database/migration.py`
**Impact radius**: ~6 files

**Problem**: Two files in the same directory differ by only a suffix. `migration.py` defines the base class; `migrate.py` is the runner. A developer can't tell which to import without reading both.

**Approach**: Rename `migration.py` → `migration_base.py`, `migrate.py` → `migration_runner.py`.

**Status**: pending
**Notes**:

---

### [P2] RF-013: hierarchical.py — 2086-line god-module (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/services/channel_finder/databases/hierarchical.py`
**Impact radius**: ~5 files

**Problem**: The largest non-template file in the codebase. A single class with 53 methods handling tree loading, navigation, traversal, channel expansion, validation, and serialization.

**Approach**: Extract tree navigation, channel expansion, validation, and serialization into helper modules. Keep the main class as a facade.

**Status**: pending
**Notes**:

---

### [P2] RF-014: cli/migrate_cmd.py — 1161-line god-module (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/cli/migrate_cmd.py`
**Impact radius**: ~3 files

**Problem**: 15 functions handling migration logic, version detection, file diffing, and conflict resolution in a CLI command handler. Business logic should be in a service layer.

**Approach**: Extract migration engine into `services/migration/` with separate files for version detection, diff computation, conflict resolution. CLI command becomes a thin wrapper.

**Status**: pending
**Notes**:

---

### [P2] RF-015: ariel_search/database/repository.py — 1082-line god-module (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/services/ariel_search/database/repository.py`
**Impact radius**: ~6 files

**Problem**: Single class handling all CRUD, search queries, embedding management, attachment storage, and statistics.

**Approach**: Split into domain-specific repository modules: `entry_repository.py`, `search_repository.py`, `embedding_repository.py`.

**Status**: pending
**Notes**:

---

### [P2] RF-016: cli/ariel.py — 1059 lines + 13 deep service imports (pending)

**Severity**: high | **Category**: structure, dependency
**Files**: `src/osprey/cli/ariel.py`
**Impact radius**: ~13 files

**Problem**: CLI command reaches deep into 13+ service internals (database connection, migrations, enhancement, ingestion, scheduler) rather than calling a single service facade.

**Approach**: Create an `ArielSearchManager` facade. CLI command delegates to facade methods.

**Status**: pending
**Notes**:

---

### [P2] RF-017: cli/health_cmd.py — 957-line CLI with embedded health logic (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/cli/health_cmd.py`
**Impact radius**: ~3 files

**Problem**: 22 functions performing health checks across MCP servers, connectors, databases, and services. Health check implementations should be in the service layer.

**Approach**: Extract health check implementations into a `health/` service package. CLI formats and displays results.

**Status**: pending
**Notes**:

---

### [P2] RF-018: deployment/compose_generator.py — 985-line generator (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/deployment/compose_generator.py`
**Impact radius**: ~3 files

**Problem**: 13 methods generating Docker Compose configs with service definitions, volumes, networks, and env vars all in one file.

**Approach**: Extract service-specific generators and have `compose_generator.py` orchestrate them.

**Status**: pending
**Notes**:

---

### [P2] RF-019: python_executor/container_engine.py — 958-line engine (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/services/python_executor/execution/container_engine.py`
**Impact radius**: ~4 files

**Problem**: 16 methods mixing container orchestration (image building, volume mounting) with execution logic (output capture, result parsing).

**Approach**: Split into `container_builder.py` and `container_runner.py` with `container_engine.py` as orchestrator.

**Status**: pending
**Notes**:

---

### [P2] RF-020: cli/project_actions.py — 826-line grab-bag (pending)

**Severity**: high | **Category**: structure
**Files**: `src/osprey/cli/project_actions.py`
**Impact radius**: ~3 files

**Problem**: Bundles deploy, health, tasks, export, and configuration handlers together just because they share the interactive menu entry point.

**Approach**: Split into domain-specific action modules or delegate to corresponding CLI command modules.

**Status**: pending
**Notes**:

---

### [P2] RF-021: python_executor/services.py — 730 lines + dead classes (pending)

**Severity**: medium | **Category**: structure, vestige
**Files**: `src/osprey/services/python_executor/services.py`, `src/osprey/services/python_executor/__init__.py`
**Impact radius**: ~5 files

**Problem**: `FileManager` and `NotebookManager` use the LangGraph `configurable` dict pattern and are never instantiated outside their own module. Exported in `__init__.py` but never imported externally.

**Approach**: Remove `FileManager`, `NotebookManager`, `PythonExecutionContext`, `NotebookAttempt`, `NotebookType`. Keep `serialize_results_to_file` and `make_json_serializable`.

**Status**: pending
**Notes**:

---

### [P2] RF-022: registry/ imports from connectors/ and models/ — layering violation (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/registry/initializers.py`, `src/osprey/registry/manager.py`
**Impact radius**: ~9 files

**Problem**: Registry (L6) imports `ConnectorFactory` from connectors (L5) and `get_provider_registry` from models. The registry should be a passive discovery layer, not import and instantiate what it discovers.

**Approach**: Use a plugin/entry-point pattern where connectors and models register themselves. Invert the dependency.

**Status**: pending
**Notes**:

---

### [P2] RF-023: runtime/ imports from connectors/ — layering violation (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/runtime/__init__.py`
**Impact radius**: ~3 files

**Problem**: Runtime (L7) imports `LimitsValidator` and `ConnectorFactory` from connectors (L5). Lower-level execution primitives should not import higher-level abstractions.

**Approach**: Inject connector instances into runtime functions as parameters.

**Status**: pending
**Notes**:

---

### [P2] RF-024: infrastructure/server_launcher.py imports from interfaces/ — layering violation (pending)

**Severity**: medium | **Category**: dependency
**Files**: `src/osprey/infrastructure/server_launcher.py`
**Impact radius**: ~6 files

**Problem**: Imports `create_app` from 4 interface modules. Infrastructure should not depend on the higher-level interfaces it launches.

**Approach**: Use lazy string-based imports or a factory registry pattern.

**Status**: pending
**Notes**:

---

### [P2] RF-025: Channel finder MCP registry duplication (pending)

**Severity**: medium | **Category**: cohesion
**Files**: `src/osprey/mcp_server/direct_channel_finder/registry.py`, `src/osprey/mcp_server/channel_finder_hierarchical/registry.py`, `src/osprey/mcp_server/channel_finder_in_context/registry.py`, `src/osprey/mcp_server/channel_finder_middle_layer/registry.py`
**Impact radius**: ~8 files

**Problem**: Four MCP server packages duplicate the same singleton registry pattern for config loading and backend instantiation. `direct_channel_finder/registry.py` even has its own `_load_config()` bypassing the canonical config system.

**Approach**: Extract shared channel finder MCP registry base class into `services/channel_finder/`. All four registries inherit from it.

**Status**: pending
**Notes**: Overlaps with RF-007. Consider executing together.

---

### [P3] RF-026: capability.py vs capabilities.py near-collision (pending)

**Severity**: medium | **Category**: naming
**Files**: `src/osprey/services/ariel_search/capability.py`, `src/osprey/services/ariel_search/capabilities.py`

**Problem**: Two files in the same package differ by only a trailing 's'. `capability.py` is the service singleton factory; `capabilities.py` builds frontend discovery responses. Completely unrelated concerns with near-identical names.

**Approach**: Rename `capability.py` → `service_factory.py`.

**Status**: pending
**Notes**:

---

### [P3] RF-027: "runtime" word collision (pending)

**Severity**: medium | **Category**: naming
**Files**: `src/osprey/runtime/__init__.py`, `src/osprey/deployment/runtime_helper.py`

**Problem**: "Runtime" means two different things: code execution environment vs container runtime binary (Docker/Podman).

**Approach**: Rename `deployment/runtime_helper.py` → `deployment/container_runtime.py`.

**Status**: pending
**Notes**:

---

### [P3] RF-028: Docs reference deleted modules extensively (pending)

**Severity**: high | **Category**: vestige
**Files**: `docs/source/developer-guides/01_understanding-the-framework/03_langgraph-integration.rst`, `docs/source/getting-started/conceptual-tutorial.rst`, `docs/source/getting-started/installation.rst`, `docs/source/api_reference/index.rst`

**Problem**: 48 documentation files reference LangGraph (157 occurrences). 38 files reference deleted modules (osprey.approval, osprey.data_management, osprey.state). An entire file is dedicated to the old LangGraph integration that no longer exists.

**Approach**: Large documentation rewrite effort. Priority: (1) Remove/rewrite LangGraph integration doc, (2) Update conceptual tutorial and installation, (3) Remove API references for deleted modules.

**Status**: pending
**Notes**: Large effort. Consider a separate docs sprint.

---

### [P3] RF-029: Dead should_retry_code_generation() and max_generation_retries (pending)

**Severity**: medium | **Category**: vestige
**Files**: `src/osprey/services/python_executor/exceptions.py`, `src/osprey/services/python_executor/config.py`, `src/osprey/utils/config.py`

**Problem**: `should_retry_code_generation()` was the LangGraph retry hook — never called. `max_generation_retries` config is set but never read.

**Approach**: Remove the method, config field, and default.

**Status**: pending
**Notes**:

---

### [P3] RF-030: Backward-compat shims in python_executor (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/services/python_executor/execution/limits_validator.py`, `src/osprey/services/python_executor/exceptions.py`

**Problem**: `limits_validator.py` is a one-line re-export shim with zero importers. `ChannelLimitsViolationError` re-export in `exceptions.py` is also unused.

**Approach**: Delete `limits_validator.py`. Remove the backward-compat import from `exceptions.py`.

**Status**: pending
**Notes**:

---

### [P3] RF-031: Dead code_generator infrastructure (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/cli/interactive_menu.py`, `src/osprey/cli/init_wizard.py`, `src/osprey/utils/config.py`

**Problem**: `get_code_generator_metadata()` always returns empty dict. `select_code_generator()` always shows "No code generators available". Config defaults still reference `code_generator: basic`.

**Approach**: Remove both functions and the dead config defaults.

**Status**: pending
**Notes**:

---

### [P3] RF-032: epics_writes_enabled backward-compat field (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/services/python_executor/execution/control.py`, `src/osprey/services/python_executor/models.py`, `src/osprey/utils/config.py`

**Problem**: Deprecated `epics_writes_enabled` field kept alongside the new `control_system_writes_enabled` with a `__post_init__` bridge.

**Approach**: Remove `epics_writes_enabled`, update all callers to use `control_system_writes_enabled`.

**Status**: pending
**Notes**:

---

### [P3] RF-033: Legacy server/agent config deprecation handlers (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/cli/server_registry.py`

**Problem**: `DeprecationWarning` handlers for `disable_servers`, `extra_servers`, `disable_agents`. `_custom_server_from_legacy()` exists solely for the deprecated format.

**Approach**: Evaluate if any deployments still use the old format. If not, remove.

**Status**: pending
**Notes**:

---

### [P3] RF-034: Single-file packages that should be plain modules (pending)

**Severity**: low | **Category**: structure
**Files**: `src/osprey/runtime/__init__.py`, `src/osprey/infrastructure/`, `src/osprey/interfaces/agentsview/`, `src/osprey/interfaces/tuning/`, `src/osprey/mcp_server/export/`

**Problem**: Several directories contain only `__init__.py` and one implementation file, or have all code in `__init__.py`. The package wrapper adds no organizational value.

**Approach**: Evaluate each case — flatten to plain modules where the package is unlikely to grow.

**Status**: pending
**Notes**: Low risk but also low urgency.

---

### [P3] RF-035: osprey.context placeholder in execution wrapper (pending)

**Severity**: low | **Category**: vestige
**Files**: `src/osprey/services/python_executor/execution/wrapper.py`

**Problem**: `_get_context_loading()` sets `context = None` as a placeholder for the deleted `osprey.context` module. This injects dead code into every wrapped script execution.

**Approach**: Remove `_get_context_loading()` and its call site.

**Status**: pending
**Notes**:
