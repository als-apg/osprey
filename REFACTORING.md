# Refactoring Backlog

> Auto-generated by `/refactor scan` on 2026-02-28.
> Use `/refactor next` to execute items. Use `/refactor status` to check progress.

## Summary

- Total items: 27
- Completed: 3
- Remaining: 24
- P0: 2 | P1: 10 | P2: 9 | P3: 6

## Backlog

### [P0] RF-001: Split mcp_server/common.py into focused modules (completed 2026-02-28)

**Severity**: high | **Categories**: structure, dependency, cohesion
**Files**: `src/osprey/mcp_server/common.py`
**Impact radius**: 69 files import from this module
**Confirmed by**: Structure, Dependency, Cohesion agents

**Problem**: `common.py` (420 lines) is imported by 69 files across 4 top-level modules (mcp_server: 50, interfaces: 11, services: 6, cli: 2). It bundles 10+ unrelated responsibilities: startup timing, logging configuration, config loading with caching, workspace resolution, error envelopes, HTTP utilities (post_json, notify_panel_focus), session metadata gathering, URL builders, server entry point, and workspace singleton init. Generic utilities like `load_osprey_config` and `resolve_workspace_root` force services/ and interfaces/ to depend upward on the mcp_server layer.

**Approach**:
1. Split into focused modules: `mcp_server/startup.py` (run_mcp_server, startup_timer, redirect_logging_to_stderr), `mcp_server/config.py` (load_osprey_config, resolve_config_path, resolve_workspace_root), `mcp_server/errors.py` (make_error), `mcp_server/http.py` (post_json, notify_panel_focus, gallery_url, web_terminal_url), `mcp_server/session.py` (gather_session_metadata)
2. Move generic utilities (load_osprey_config, resolve_workspace_root) to `osprey.utils` to eliminate ~17 cross-layer imports
3. Keep `common.py` as a backward-compat re-export shim temporarily
4. Update direct importers one module at a time, then remove shim

**Status**: completed (2026-02-28) — Split into `mcp_server/{errors,startup,http,session}.py` + `utils/workspace.py`. Shim removed. All 81 importers migrated.

---

### [P0] RF-002: Move generators/config_updater.py to utils/ (completed 2026-02-28)

**Severity**: high | **Categories**: structure, naming, cohesion
**Files**: `src/osprey/generators/__init__.py`, `src/osprey/generators/config_updater.py`
**Impact radius**: 2 files (cli/interactive_menu.py, cli/config_cmd.py)
**Confirmed by**: Structure, Naming, Cohesion agents

**Problem**: The `generators/` package is a thin wrapper around a single module (config_updater.py, 498 lines). The name "generators" implies code generation (IOC templates, scaffolding) but the actual content is YAML file update utilities. The previous code generation modules were deleted during migration. All callers are CLI commands. Three independent agents flagged this from different angles: structural redundancy, misleading name, and misplaced code.

**Approach**:
1. Move `generators/config_updater.py` to `utils/config_updater.py`
2. Update all imports (grep for `from osprey.generators`)
3. Delete the `generators/` package
4. Update `__init__.py` docstring if needed

**Status**: completed (2026-02-28) — Moved config_updater.py to utils/, updated 6 source imports + 4 patch() strings in tests, moved 3 test files from tests/generators/ to tests/utils/, deleted generators/ package entirely. No backward-compat shim needed. All 3584 tests pass.

---

### [P1] RF-003: Extract timeseries utilities from interfaces layer (completed 2026-02-28)

**Severity**: high | **Categories**: dependency, cohesion
**Files**: `src/osprey/interfaces/artifacts/app.py`, `src/osprey/mcp_server/workspace/tools/archiver_downsample.py`
**Impact radius**: 3 files

**Problem**: `archiver_downsample.py` (an MCP server tool) imports `_extract_timeseries_frame` and `lttb_downsample` directly from `interfaces/artifacts/app.py` (a Flask app). This is a layering violation — MCP server tools should never import from HTTP interfaces. These are general-purpose timeseries utilities that happen to live in a Flask app file.

**Approach**:
1. Create `src/osprey/utils/timeseries.py` with `lttb_downsample` and `extract_timeseries_frame`
2. Update both `interfaces/artifacts/app.py` and `mcp_server/workspace/tools/archiver_downsample.py` to import from the new location

**Status**: completed (2026-02-28) — Extracted `lttb_downsample` and `extract_timeseries_frame` (renamed from `_extract_timeseries_frame`) to `utils/timeseries.py`. Updated 3 source files and 5 test imports. Removed `import math` from `app.py` (only used by extracted code). No backward-compat shim needed.

---

### [P1] RF-004: Move LimitsValidator to connectors/control_system/ (completed)

**Severity**: high | **Categories**: dependency, cohesion
**Files**: `src/osprey/services/python_executor/execution/limits_validator.py`, `src/osprey/services/python_executor/exceptions.py`
**Impact radius**: 9+ files across 5 layers
**Confirmed by**: Dependency, Cohesion agents

**Problem**: `LimitsValidator` and `ChannelLimitsViolationError` are placed in `services/python_executor/` but are imported across 5 layers: runtime/ (L2), registry/ (L3), connectors/ (L4), mcp_server/ (L6), and templates/. This creates severe layering violations — connectors (L4) and runtime (L2) reach up into services (L5) for a safety-critical component. The import path `osprey.services.python_executor.execution.limits_validator` obscures that this is a framework-level safety primitive, not a Python-executor-specific concern.

**Status**: completed (2026-02-28) — Moved `LimitsValidator` to `connectors/control_system/limits_validator.py` and `ChannelLimitsViolationError` to `errors.py` (inherits from `Exception` directly). Updated 13 source files + 3 test files. Backward-compat re-exports in `services/python_executor/exceptions.py` and `services/python_executor/execution/limits_validator.py`.

---

### [P1] RF-005: Consolidate MCP servers under mcp_server/ (completed)

**Severity**: high | **Categories**: dependency, cohesion
**Files**: `src/osprey/interfaces/ariel/mcp/`, `src/osprey/services/channel_finder/mcp/`
**Impact radius**: 9 server packages, ~12 files
**Confirmed by**: Dependency, Cohesion agents

**Problem**: 9 FastMCP servers are scattered across 3 top-level packages: `mcp_server/` (5 servers), `interfaces/ariel/mcp/` (1 server), and `services/channel_finder/mcp/` (3 servers). All share identical patterns (FastMCP framework, __main__.py/server.py/tools/ structure, import from `mcp_server.common`). The ARIEL MCP server is nested inside the Flask web app package. The channel finder MCP servers are nested inside the service layer. This creates layering violations: services/ imports from mcp_server/, and mcp_server/ would need to know about interfaces/.

**Approach**:
1. Move `interfaces/ariel/mcp/` to `mcp_server/ariel/`
2. Move `services/channel_finder/mcp/hierarchical/` to `mcp_server/channel_finder_hierarchical/`
3. Move `services/channel_finder/mcp/in_context/` to `mcp_server/channel_finder_in_context/`
4. Move `services/channel_finder/mcp/middle_layer/` to `mcp_server/channel_finder_middle_layer/`
5. Update server_registry.py entry points and all imports

**Status**: completed (2026-02-28) — Moved 42 source files and 33 test files. All 9 MCP servers now live under `mcp_server/`. Updated imports in `server_registry.py`, `logbook.py`, `channel_finder/app.py`, `channel_finder/database_api.py`, and 3 test files. No backward-compat shims (all consumers are internal). 3587 tests pass.

**Status**: pending

---

### [P1] RF-006: Consolidate config loading systems (pending)

**Severity**: high | **Categories**: dependency, cohesion
**Files**: `src/osprey/utils/config.py`, `src/osprey/mcp_server/common.py`, `src/osprey/services/channel_finder/utils/config.py`
**Impact radius**: 28+ files
**Confirmed by**: Dependency, Cohesion agents

**Problem**: Three independent config loading systems exist: (1) `utils/config.py` with ConfigBuilder singleton and `get_config_value()` (28 importers), (2) `mcp_server/common.py` with `load_osprey_config()` and its own `_config_cache` (used by interfaces and MCP servers), (3) `services/channel_finder/utils/config.py` as a thin 37-line wrapper. The two main caches are independent, so `load_osprey_config()` and `get_config_value()` can return inconsistent results if one cache is cleared but not the other. Additionally, `utils/config.py` imports `VALID_TIERS` from `cli/claude_code_resolver` — a severe layering violation (L1 utils → L8 CLI).

**Approach**:
1. Move `VALID_TIERS` from `cli/claude_code_resolver` to `utils/config.py` or `utils/constants.py` to fix the layering violation
2. Replace `load_osprey_config()` in `mcp_server/common.py` with a thin wrapper around `get_config_builder().raw_config` using the same cache
3. Delete `services/channel_finder/utils/config.py` — have callers import directly from `osprey.utils.config`
4. Consolidate env-var resolution (duplicate implementations in `utils/config.py` and `mcp_server/introspect.py`)

**Status**: pending

---

### [P1] RF-007: Move server_launcher.py out of mcp_server/ (pending)

**Severity**: high | **Categories**: dependency, cohesion
**Files**: `src/osprey/mcp_server/server_launcher.py`
**Impact radius**: 4 files (workspace tools that import launcher functions)
**Confirmed by**: Dependency, Cohesion agents

**Problem**: `server_launcher.py` lives in `mcp_server/` but launches non-MCP HTTP servers: Artifact Gallery (FastAPI), ARIEL web app (Flask), Tuning panel, DePlot service, and Channel Finder UI. It imports from `interfaces/artifacts/`, `interfaces/ariel/`, `interfaces/tuning/`, `interfaces/channel_finder/`, and `services/deplot/` — creating layering violations where the MCP layer depends on the interfaces layer. None of these are MCP servers.

**Approach**:
1. Move `server_launcher.py` to `src/osprey/infrastructure/server_launcher.py` or `src/osprey/services/launcher.py`
2. Update the 4 workspace tool importers
3. This naturally resolves the mcp_server → interfaces layering violations

**Status**: pending

---

### [P1] RF-008: Split cli/interactive_menu.py (2591 lines) (pending)

**Severity**: high | **Categories**: structure, dependency
**Files**: `src/osprey/cli/interactive_menu.py`
**Impact radius**: ~4 files (CLI entry points)
**Confirmed by**: Structure, Dependency agents

**Problem**: At 2,591 lines with 35+ top-level functions, this is the largest file in the codebase. It imports from 7 distinct top-level modules (cli, deployment, generators, models, registry, templates, utils). It covers: banner display, project discovery, provider/model selection, API key configuration, deploy handling, health checking, task launching, export action, and the full interactive init wizard.

**Approach**:
1. Extract `cli/init_wizard.py` — the interactive init flow (largest coherent chunk)
2. Extract `cli/project_actions.py` — deploy/health/tasks/export handlers
3. Extract `cli/menu_display.py` — banner, ASCII art, menu rendering helpers
4. Keep `interactive_menu.py` as the thin orchestrator that imports from these

**Status**: pending

---

### [P1] RF-009: Split deployment/container_manager.py (1442 lines) (pending)

**Severity**: high | **Categories**: structure, dependency
**Files**: `src/osprey/deployment/container_manager.py`
**Impact radius**: ~5 files
**Confirmed by**: Structure, Dependency agents

**Problem**: At 1442 lines, `container_manager.py` handles all container orchestration: Docker/Podman interaction, compose file generation, health checking, and lifecycle management. It also contains a layering violation — importing `Styles` and `console` from `osprey.cli.styles` (deployment should not depend on CLI presentation).

**Approach**:
1. Extract `deployment/compose_generator.py` (compose file creation)
2. Extract `deployment/container_lifecycle.py` (start/stop/restart)
3. Extract `deployment/health_checker.py` (health monitoring)
4. Keep `container_manager.py` as the public orchestrator
5. Replace `cli.styles` import with standard logging or a passed callback

**Status**: pending

---

### [P1] RF-010: Split registry/manager.py (1398 lines) + fix layering (pending)

**Severity**: high | **Categories**: structure, dependency
**Files**: `src/osprey/registry/manager.py`
**Impact radius**: ~8 files
**Confirmed by**: Structure, Dependency agents

**Problem**: `RegistryManager` at 1398 lines handles component discovery, registration, validation, and lazy loading in one file. It also has layering violations — the registry (L3) imports from connectors (L4), models (L5), and services/python_executor (L5) to wire components together. The registry should be a framework for higher layers to register into, not a layer that reaches upward.

**Approach**:
1. Extract `registry/validation.py` (validation logic) and `registry/loader.py` (lazy-loading mechanics)
2. Use lazy registration pattern: higher layers register their factories with the registry at import time
3. Or reclassify registry as a higher-level orchestration layer above services

**Status**: pending

---

### [P1] RF-011: Add BaseStore.update_entry_metadata() public API (pending)

**Severity**: medium | **Categories**: cohesion
**Files**: `src/osprey/mcp_server/base_store.py`, `src/osprey/mcp_server/workspace/tools/_viz_common.py`, `src/osprey/mcp_server/workspace/tools/submit_response.py`, `src/osprey/mcp_server/workspace/tools/create_document.py`
**Impact radius**: 3 files with identical TODOs

**Problem**: Three workspace tools call the private method `store._save_index()` directly, each with the same TODO comment: "Replace with a public API method once BaseStore exposes one."

**Approach**:
1. Add `BaseStore.update_entry_metadata(entry_id, **kwargs)` that sets attributes and calls `_save_index()`
2. Update the 3 callers to use the new public method
3. Remove the TODO comments

**Status**: pending

---

### [P1] RF-012: Consolidate exception hierarchy (pending)

**Severity**: medium | **Categories**: naming, cohesion
**Files**: `src/osprey/errors.py`, `src/osprey/services/channel_finder/core/exceptions.py`, `src/osprey/services/ariel_search/exceptions.py`, `src/osprey/services/python_executor/exceptions.py`
**Impact radius**: ~10 files
**Confirmed by**: Naming, Cohesion agents

**Problem**: Three separate `ConfigurationError` classes exist with different constructors (the ARIEL version requires `ErrorCategory`). `ChannelLimitsViolationError` is a framework-level safety exception nested in a service module. The framework-level `errors.py` is thin (22 lines, 2 exceptions) and only used by one file. Code catching `ConfigurationError` must use fully-qualified imports to avoid shadowing.

**Approach**:
1. Rename domain-specific versions to `ARIELConfigurationError` and `ChannelFinderConfigurationError`
2. Move `ChannelLimitsViolationError` to `osprey/errors.py` (framework-level)
3. Expand `osprey.errors` as the true framework exception base
4. Optionally have service exceptions inherit from a base in `errors.py`

**Status**: pending

---

### [P2] RF-013: Split services/channel_finder/databases/hierarchical.py (2086 lines) (pending)

**Severity**: high | **Categories**: structure
**Files**: `src/osprey/services/channel_finder/databases/hierarchical.py`
**Impact radius**: ~4 files

**Problem**: This single file implements the entire hierarchical channel finder database at 2086 lines — data parsing, tree construction, query logic, and formatting. The second-largest file in the codebase.

**Approach**:
1. Extract `tree_builder.py` (hierarchy construction)
2. Extract `query_engine.py` (search/filtering)
3. Extract `formatters.py` (output formatting)
4. Keep main module as a facade

**Status**: pending

---

### [P2] RF-014: Split cli/templates.py (1930 lines) (pending)

**Severity**: high | **Categories**: structure
**Files**: `src/osprey/cli/templates.py`
**Impact radius**: ~5 files

**Problem**: Template rendering, scaffolding, and project generation in a single 1930-line file. Third-largest file in the codebase.

**Approach**:
1. Extract `template_registry.py` (discovery/listing)
2. Extract `template_renderer.py` (Jinja2 rendering)
3. Extract `scaffold.py` (directory/file creation)

**Status**: pending

---

### [P2] RF-015: Split interfaces/web_terminal/routes.py (1254 lines) (pending)

**Severity**: high | **Categories**: structure
**Files**: `src/osprey/interfaces/web_terminal/routes.py`
**Impact radius**: ~3 files

**Problem**: All web terminal HTTP/WebSocket routes in a single 1254-line file, mixing endpoint definitions, request validation, business logic, and response formatting.

**Approach**:
1. Split routes by domain: `routes/terminal.py` (PTY/WebSocket), `routes/files.py` (file operations), `routes/sessions.py` (session management)
2. Use FastAPI router per module and include them in the main app

**Status**: pending

---

### [P2] RF-016: Split interfaces/channel_finder/database_api.py (994 lines) (pending)

**Severity**: high | **Categories**: structure, dependency
**Files**: `src/osprey/interfaces/channel_finder/database_api.py`
**Impact radius**: ~5 files

**Problem**: At 994 lines with 37 osprey imports, this file directly imports tool functions from 3 different MCP server variants, bypassing the MCP protocol entirely to call tool implementations as regular functions. Extreme fan-out (14 unique osprey modules).

**Approach**:
1. Create a unified channel finder service facade that exposes needed operations
2. Have `database_api.py` import from a single service interface rather than reaching into MCP tool internals across 3 pipeline variants
3. Split remaining routes by resource type

**Status**: pending

---

### [P2] RF-017: Consolidate duplicate Rich logging setup (pending)

**Severity**: medium | **Categories**: cohesion
**Files**: `src/osprey/utils/logger.py`, `src/osprey/mcp_server/common.py`
**Impact radius**: ~10 files (MCP server __main__.py files)

**Problem**: Rich logging initialization is implemented twice with near-identical code. `utils/logger.py` has `_setup_rich_logging()` targeting stdout. `mcp_server/common.py` has `redirect_logging_to_stderr()` doing the exact same thing targeting stderr. Both check for existing RichHandler, set force_terminal=True and width=120, and suppress the same list of loggers (httpx, httpcore, requests, urllib3, LiteLLM).

**Approach**:
1. Add a `stderr` parameter to `_setup_rich_logging()` in `utils/logger.py` (default False)
2. Have `redirect_logging_to_stderr()` call `_setup_rich_logging(stderr=True)`
3. Eliminate the duplicate code

**Status**: pending

---

### [P2] RF-018: Move notebook_renderer and export/ to shared location (pending)

**Severity**: medium | **Categories**: cohesion
**Files**: `src/osprey/mcp_server/notebook_renderer.py`, `src/osprey/mcp_server/export/converter.py`
**Impact radius**: 3 files

**Problem**: `notebook_renderer.py` (creates notebooks, renders to HTML) and `export/converter.py` (converts HTML to images via Playwright) live in `mcp_server/` but are primarily consumed by `interfaces/artifacts/` and `interfaces/ariel/mcp/`. These are general-purpose rendering utilities, not MCP-specific.

**Approach**:
1. Move to `src/osprey/utils/notebook_renderer.py` and `src/osprey/utils/export/`
2. Or create `src/osprey/rendering/` package for all rendering concerns
3. Update importers in both mcp_server and interfaces

**Status**: pending

---

### [P2] RF-019: Move type_registry and base_store to shared workspace layer (pending)

**Severity**: medium | **Categories**: cohesion
**Files**: `src/osprey/mcp_server/type_registry.py`, `src/osprey/mcp_server/base_store.py`, `src/osprey/mcp_server/artifact_store.py`
**Impact radius**: 6 files

**Problem**: `type_registry.py` (artifact/category/tool type definitions), `base_store.py` (file-backed indexed store), and `artifact_store.py` (ArtifactStore singleton) live in `mcp_server/` but are heavily consumed by `interfaces/artifacts/` (gallery web app). The `type_registry.py` defines display labels and colors for the gallery's badge system — nothing MCP-specific. Both mcp_server/ tools and interfaces/ apps depend on these.

**Approach**:
1. Create `src/osprey/stores/` or `src/osprey/workspace/` package
2. Move `base_store.py`, `artifact_store.py`, and `type_registry.py` there
3. Both mcp_server/ and interfaces/ import from the neutral shared layer

**Status**: pending

---

### [P2] RF-020: Rename cli/server_registry.py to avoid concept collision (pending)

**Severity**: medium | **Categories**: naming
**Files**: `src/osprey/cli/server_registry.py`
**Impact radius**: ~3 files

**Problem**: "registry" means two different things: `osprey.registry` is the component registry (RegistryManager), while `osprey.cli.server_registry` is a catalog of MCP server definitions for Claude Code config generation.

**Approach**:
1. Rename `cli/server_registry.py` to `cli/mcp_server_definitions.py` (or `claude_code_servers.py`)
2. Update all imports

**Status**: pending

---

### [P2] RF-021: Rename ariel_search/capability.py to resolve near-collision (pending)

**Severity**: medium | **Categories**: naming
**Files**: `src/osprey/services/ariel_search/capability.py`, `src/osprey/services/ariel_search/capabilities.py`
**Impact radius**: ~5 files

**Problem**: `capability.py` (singleton service factory) vs `capabilities.py` (search-mode metadata for frontend). The one-letter difference doesn't communicate the completely different purposes.

**Approach**:
1. Rename `capability.py` to `service_factory.py`
2. Update all imports

**Status**: pending

---

### [P3] RF-022: Rename "models/" package to "llm/" or "inference/" (pending)

**Severity**: medium | **Categories**: naming
**Files**: `src/osprey/models/__init__.py`, `src/osprey/models/completion.py`, `src/osprey/models/providers/`
**Impact radius**: ~15 files

**Problem**: The top-level `models/` package conventionally implies data models (ORMs, Pydantic schemas) but actually contains the LLM inference layer: chat completion, provider adapters (Anthropic, OpenAI, Google), and embedding interfaces. Actual data models live in `services/*/models.py`. A developer looking for data models finds LLM code instead.

**Approach**:
1. Rename `models/` to `llm/` or `inference/`
2. Update all imports (`from osprey.models` → `from osprey.llm`)
3. Wide impact but mechanical rename

**Status**: pending

---

### [P3] RF-023: Clean up LangGraph documentation vestiges (pending)

**Severity**: medium | **Categories**: vestige
**Files**: 48 docs files including `docs/source/developer-guides/01_understanding-the-framework/03_langgraph-integration.rst`
**Impact radius**: 0 (documentation only)

**Problem**: 48 documentation files reference LangGraph as the orchestration foundation ("built on LangGraph", "LangGraph-compatible wrapper", "LangGraph React agent"). An entire page (`03_langgraph-integration.rst`) describes StateGraph and checkpoint-based persistence. The framework now uses Claude Code + MCP, not LangGraph. Additionally, `docs/source/conf.py` mocks `langgraph`, `langchain`, and `langchain_core` for autodoc despite them no longer being dependencies.

**Approach**:
1. Remove or rewrite `03_langgraph-integration.rst`
2. Audit all 48 files, replace LangGraph descriptions with Claude Code + MCP architecture
3. Remove LangGraph/LangChain entries from `autodoc_mock_imports` in `conf.py`

**Status**: pending

---

### [P3] RF-024: Remove documentation references to deleted modules (pending)

**Severity**: medium | **Categories**: vestige
**Files**: 25+ docs files referencing `osprey.base`, `osprey.context`, `osprey.approval`, `osprey.capabilities`
**Impact radius**: 0 (documentation only, but doc builds may fail)

**Problem**: API reference pages contain `autoclass`/`automethod` directives for deleted modules (`osprey.base.BaseCapability`, `osprey.context.ContextManager`, `osprey.approval.*`, `osprey.capabilities.*`). These will fail during doc builds. Tutorial pages contain example imports from these deleted modules.

**Approach**:
1. Remove or rewrite API reference pages for deleted modules
2. Update tutorials to use current module paths
3. Update parent `index.rst` toctrees

**Status**: pending

---

### [P3] RF-025: Remove epics_writes_enabled backward-compat shim (pending)

**Severity**: medium | **Categories**: vestige
**Files**: `src/osprey/services/python_executor/execution/control.py`, `src/osprey/services/python_executor/models.py`, `src/osprey/utils/config.py`
**Impact radius**: ~5 files

**Problem**: `ExecutionControlConfig` maintains a deprecated `epics_writes_enabled` field with `__post_init__` backward-compat logic that copies to `control_system_writes_enabled`. The config layer emits both keys with the same value. `get_execution_mode()` parameters use EPICS-specific naming (`has_epics_writes/reads`) despite being protocol-agnostic. This was for the EPICS-to-protocol-agnostic migration that is now complete.

**Approach**:
1. Remove `epics_writes_enabled` field and `__post_init__` compat logic
2. Rename `has_epics_writes/reads` parameters to `has_control_system_writes/reads`
3. Remove duplicate config key emission
4. Update all call sites

**Status**: pending

---

### [P3] RF-026: Clean up stale code comments and docstrings (pending)

**Severity**: low | **Categories**: vestige
**Files**: `src/osprey/services/python_executor/execution/wrapper.py`, `src/osprey/registry/base.py`, `src/osprey/services/ariel_search/search/base.py`, `src/osprey/services/ariel_search/search/sql_query.py`
**Impact radius**: 0 (comment-only changes)

**Problem**: Multiple stale references to the old architecture remain in code comments and docstrings:
- `wrapper.py:139`: "FIXES THE CURRENT BUG!" unexplained workaround marker
- `wrapper.py:490-496`: `_get_context_loading()` sets `context = None` as placeholder for deleted `osprey.context`
- `registry/base.py:15-19`: ServiceRegistration docstring describes "internal service graphs" and "node flow" from LangGraph
- `ariel_search/search/base.py`: SearchToolDescriptor references "LangChain tools"
- `ariel_search/search/sql_query.py`: References "LangChain agent executor"

**Approach**:
1. Review each comment in context
2. Update docstrings to reference current MCP architecture
3. Remove or clarify workaround markers
4. Remove `_get_context_loading()` if no code paths use the `context` variable

**Status**: pending

---

### [P3] RF-027: Rename ariel_search/database/migrate.py vs migration.py (pending)

**Severity**: low | **Categories**: naming
**Files**: `src/osprey/services/ariel_search/database/migrate.py`, `src/osprey/services/ariel_search/database/migration.py`
**Impact radius**: ~2 files

**Problem**: Two files with confusingly similar names: `migration.py` defines `BaseMigration` (the framework) while `migrate.py` defines `MigrationRunner` (the executor). The verb/noun distinction is too subtle.

**Approach**:
1. Rename `migration.py` to `migration_base.py`
2. Rename `migrate.py` to `migration_runner.py`
3. Update all imports

**Status**: pending
