"""Baseline fixtures: the rendered ``build/config.yml`` documents, per preset and channel-finder mode.

Regenerated only by an explicit decision; see :mod:`freeze` for what is captured,
what is masked, and how to point it at a pristine source tree.
"""
